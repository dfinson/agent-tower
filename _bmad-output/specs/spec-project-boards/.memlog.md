---
topic: Multi-project board organization for CodePlane's job dashboard (Option A: repo-scoped board tabs; Option B: projects overview with drill-in)
updated: 2026-08-10T12:03
---

- (note) Origin: UX audit found the single flat KanbanBoard.tsx pools all jobs across all repos into one board (In Progress / Awaiting Input / Failed columns), mirroring the same flaw observed in the GitHub Copilot app itself where sessions list flat across projects.
- (note) Job model already carries repo, branch, and pr_url fields (backend/models/db.py); no schema change needed for repo-scoped grouping.
- (decision) Scope this spec to Option A (repo-scoped boards) and Option B (projects overview with drill-in), the two lowest-effort, additive options from the prior UX audit; Option C (cross-repo swimlane grid) is out of scope, deferred as a future iteration.
- (constraint) Must not change Job/JobSummary schema or break existing single-board consumers (KanbanBoard, MobileJobList, e2e tests in frontend/e2e) during rollout.
- (constraint) Must reuse existing selectors/patterns (selectActiveJobs, selectSignoffJobs, selectAttentionJobs, useShallow) rather than introducing a parallel state model.
- (constraint) Option B repo summary counts must be computed from data already fetched by DashboardScreen (fetchJobs) or a small additive aggregate endpoint; no new heavy backend service.
- (capability) CAP-1 Repo-scoped board (Option A): user can filter the existing Kanban to one repo via a tab/selector so each repo gets its own In Progress/Awaiting Input/Failed board; success = selecting a repo tab shows only that repo's jobs in all three columns and persists selection across a page refresh.
- (capability) CAP-2 Projects overview (Option B): user lands on a ProjectsOverview screen showing one card per repo with active/awaiting/failed counts and last-activity time; success = clicking a card navigates to that repo's filtered board (CAP-1) and counts match the underlying job list.
- (capability) CAP-3 Cross-repo attention rollup: the overview surfaces total counts needing attention (awaiting input + failed) across all repos in one glance without opening each board; success = a single number/badge is visible on the overview screen before drill-in.
- (capability) CAP-4 Repo registry as board source of truth: the set of repo tabs/cards on both screens is driven by codeplane_repo list (registered repos), not just repos that happen to have jobs, so an empty repo still shows a card; success = registering a new repo with zero jobs immediately shows an empty board/card.
- (assumption) Assumed job counts per repo are cheap to compute client-side from the already-fetched job list (current job volumes are small); a dedicated backend aggregate endpoint is proposed as an optional optimization, not a hard requirement.
- (open_question) Should repo selection state (Option A) live in the URL (e.g. /?repo=foo) for shareable/bookmarkable links, or in client-only UI state? Recommend URL for shareability but needs confirmation.
- (open_question) For users with only 1 registered repo, should Option B overview be skipped entirely and go straight to the board, to avoid an extra click?
- (event) Self-validate pass 1 (coherence, rules 1-6,8): PASS. 4 capabilities each have intent+success; intents are WHAT not HOW; constraints (schema, selector reuse, no heavy backend) each rule something out; 2 explicit non-goals present; success signal is demonstrable (click-count + isolation test); CAP IDs stable/sequential; prose is lean.
- (event) Self-validate pass 2 (preservation): PASS. Every load-bearing claim from the prior UX-audit conversation (flat board pain, repo/branch/pr_url already on Job model, Option A/B scope, Option C deferral, selector reuse constraint, URL vs client state question, 1-repo skip question) landed in SPEC.md or ui-flows.md companion. No wrapper-only drops identified.
- (event) Spec finalized: spec-project-boards, 4 capabilities, 1 companion (ui-flows.md), 2 open questions, 1 assumption.
- (decision) Resolved open question: selected repo (CAP-1) lives in the URL query string (e.g. /?repo=foo) for shareable/bookmarkable links, not client-only state.
- (decision) Resolved open question: CAP-2 Projects Overview is always shown, even for users with only one registered repo; it is not skipped for the single-repo case.
- (decision) [ADOPTED from architecture-project-boards spine, AD-2] CAP-1 is implemented as a new child route of the existing /repos/:repoPath shell (RepoLayout), e.g. /repos/:repoPath/board -- reusing existing per-repo sidebar chrome -- rather than a tab strip inside DashboardScreen's KanbanBoard.
- (decision) [ADOPTED from architecture-project-boards spine, AD-3] CAP-2/CAP-3 render at the bare /repos index route (ProjectsOverview), replacing RepoLayout's current silent auto-redirect-to-first-repo, rather than as a new top-level screen outside the /repos shell.
- (note) Brownfield discovery folded in from architecture spine: /repos/:repoPath/* (RepoLayout, RepoOverview, RepoJobs, RepoHealth, RepoCost, RepoSettings) and GET /settings/repos/{repo}/summary already exist; RepoJobs is a separate analytics table (fetchAnalyticsJobs), left untouched and distinct from the new CAP-1 board.
- (constraint) GET /settings/repos/{repo}/summary must be extended with awaitingInputCount and failedCount (additive) and a new batch GET /settings/repos/summary added for CAP-2/CAP-3, per architecture AD-3/AD-4 -- avoids N+1 fetches and duplicate hand-written types.
- (event) SPEC updated post-architecture: CAP-1/CAP-2 integration point resolved to the existing /repos shell; both former architecture [ASSUMPTION]s promoted to adopted decisions.
- (decision) Option C (cross-repo swimlane grid) is rejected, not deferred -- remove it from SPEC.md entirely, including from Non-goals. It should not appear in positive or negative terms in any rendered artifact.
- (decision) Scope expansion (post-mockup review): introduce Project as a new first-class grouping entity above Repo. A Project is a named collection of one or more resources (repo path(s), plus optional external tracker links: GitHub Project, Jira project/board, Azure DevOps project) rather than assuming 1 repo = 1 board unit. Reason: real usage often ties multiple repos to one Jira/AzDO project, or a GH Project spans repos. This changes CAP-2/CAP-3's unit of grouping from repo to project, and CAP-1's board scope from repo to project (all its repos jobs combined).
- (decision) Add CAP-5: repo/project filter and search on the Projects Overview (/repos index) - a text filter box that narrows the card grid by name, plus filtering in the existing sidebar list. Gap found during mobile review: no search/filter existed anywhere in the design despite users managing 3+ repos being the explicit target persona.
- (decision) Add CAP-6: Project registry CRUD (create/edit a Project: name, member repo(s), optional external links) as a new settings surface, additive to the existing per-repo registration flow. A repo not yet assigned to any Project is auto-wrapped in an implicit 1:1 Project (its own name) so CAP-1-4 keep working unchanged for the common single-repo case; no forced migration step for existing users.
- (constraint) Project is additive: existing Job.repo/branch/pr_url fields and the codeplane_repo registry are untouched; a new ProjectRow (id, name, repo_paths: list[str], external_links: list of type/url) is introduced, with repos able to belong to at most one explicit Project (enforced) to avoid double-counting job totals across overlapping projects.
- (open_question) External link types to support at v1: GitHub Project, Jira project, Azure DevOps project. Confirm this is the right initial set and whether links are just a display chip (deep-link out) or drive any in-app data (e.g. pulling Jira ticket counts) - recommend display-chip-only for v1, no live tracker API integration, to keep scope additive.
- (decision) Strategic decision (researched): external tracker integration (Jira/Azure DevOps/GitHub Projects) is a real bidirectional capability of CodePlane, not just link metadata. Auth mechanism is locally-stored encrypted PATs per provider (not OAuth), because OAuth for all three providers requires maintainer-hosted public redirect infrastructure and per-provider app registration/review relationships (Atlassian requires a single Marketplace-reviewed 3LO app; Azure DevOps native OAuth is deprecated since April 2025, forcing Microsoft Entra ID enterprise app registration requiring org-admin consent) which is structurally incompatible with CodePlane's local-first, no-hosted-backend model. PATs also work for on-prem Azure DevOps Server which has no OAuth support at all.
- (decision) Add CAP-7: bidirectional external tracker sync. CodePlane polls each connected provider (Jira/AzDO/GH Projects) on an interval for linked tickets/items tied to a Project, surfaces their state (ticket counts, status) on the Project card/board, and can create/update a ticket (e.g. transition status, add a comment) when a job completes or changes state - user-configurable per Project, off by default.
- (constraint) PATs are encrypted at rest in CodePlane's local DB (not plaintext config), scoped per Project per provider, and never logged or included in job prompts/context sent to the coding agent. Sync is poll-based (no inbound webhooks, since CodePlane has no public endpoint), with a configurable interval and manual refresh.
- (note) This capability set (CAP-6/CAP-7, external tracker integration) is materially larger than the original read/navigation-layer scope (CAP-1-4). The prior non-goal 'no change to job execution/state machine' still holds, but the spec's Why/Non-goals must be revised to reflect that this is no longer purely a navigation-layer change - it now includes new external integrations, credential storage, and a sync engine.
- (decision) External tracker write-backs (CAP-7) are routed through the existing ApprovalRow/codeplane_approval flow, consistent with how CodePlane already gates other risky/irreversible job actions - not auto-applied even when a Project has sync enabled. Read-only polling (pulling ticket state in) does not require approval, only outbound writes do.
- (decision) Merge spec-task-recipes into this spec: user determined this must be one coherent spec, not two. Task Recipes CAP-1-4 (widen SidecarTemplate vocab; BMAD/spec-kit story ingestion via TaskLink; chained execution auto-spawning on the Project board; tracker_write reusing the CAP-7 approval gate) are folded in here as CAP-8-11. spec-task-recipes workspace is retired; its research/decisions are preserved by reference in this memlog, not duplicated.
- (capability) CAP-8 Widen the existing sidecar-template recipe vocabulary (phase/lifetime/scope/triggers.contextSources/triggers.outputRoutes) with lifetime=chained, outputRoutes spawn_task/tracker_write, contextSources story_node/tracker_ticket - fully backward compatible, every existing sidecar template keeps validating and running unchanged.
- (capability) CAP-9 Ingest a repo's BMAD stories or spec-kit tasks.md (read-only, never rewriting the source) into a thin TaskLink row (recipe_id, story_node_id, depends_on: list[story_node_id], job_id: nullable); re-ingestion upserts by story_node_id, no duplicate copies, no new entity paralleling Job.
- (capability) CAP-10 Render TaskLink nodes with satisfied dependencies as cards on the CAP-1 Project board; completing a linked job auto-spawns the next dependent TaskLink's job via spawn_task, each in its own worktree - Cline-style auto-chain sourced from a real BMAD/spec-kit planning artifact instead of hand-made cards.
- (capability) CAP-11 tracker_write output routes reuse CAP-7's codeplane_approval gate exactly - no second, recipe-specific approval mechanism.
- (constraint) No new atomic-run entity for recipes/chaining: TaskLink is a thin correlation row (story/task node to zero-or-one JobRow), not a parallel execution model competing with Job. Ingestion (CAP-9) is read-only against source tasks.md/story files.
- (decision) CAP-7 rescoped: integrations are NOT per-Project. Split into global Credential (provider, label, base_url, encrypted PAT - managed once in a new Settings > Integrations screen, not nested under any Project) and TrackerLink (project_id, credential_id, external_ref e.g. Jira project key / AzDO project / GitHub Project number). Many TrackerLinks can reference one Credential; a Project can hold multiple TrackerLinks (e.g. both a Jira project and a GitHub Project). This replaces the earlier per-Project-embedded-credential framing.
- (constraint) Deleting a Credential must not silently break TrackerLinks referencing it - deletion is blocked while any TrackerLink references the Credential (must unlink first), same pattern as repo removal guarding against active jobs.
- (note) Rendered the Credential/TrackerLink split into SPEC.md (Why, CAP-6, CAP-7, Constraints) and ARCHITECTURE-SPINE.md (AD-6 rewritten, AD-7 tightened to iterate per-TrackerLink, mermaid diagram, Structural Seed adds credential_service.py + IntegrationsSettings.tsx, Capability-to-Architecture map updated). ui-flows.md and the HTML mockup still show the old per-Project-embedded integration UI and need a follow-up pass.
- (note) Updated ui-flows.md (CAP-6/CAP-7 sections split into Project-settings-attaches-link vs global-Settings-Integrations, data-flow section describes trackerSummaries[] array and separate credentials endpoint) and the HTML mockup (payments card now shows 2 tracker chips sharing credentials across Projects, new Settings->Integrations screen with a global Credential list showing which/how-many Projects use each, explanatory note on the board screen) to match the AD-6 Credential/TrackerLink split. All companions now consistent with SPEC.md.
- (decision) Correction: the claim that the Copilot app's session list 'pools sessions flat across projects' is FALSE - the Copilot app actually organizes sessions per-project already. Removing this false corroborating claim from SPEC.md's Why paragraph. The CodePlane flat-board problem stands on its own evidence (KanbanBoard.tsx pooling all repos) and does not need, and should not cite, an inaccurate claim about a different product to justify it.
- (decision) Seam 1 resolved: CAP-9 ingestion is Project-scoped, not repo-scoped. Ingestion runs once per Project, iterating every member repo in project.repo_paths, parsing each repo's BMAD stories / spec-kit tasks.md independently and tagging each TaskLink with both project_id and its origin repo_path (story_node_id is only unique within one repo, so cross-repo collisions are avoided by namespacing on repo_path). depends_on edges are allowed to reference a story_node_id in a different member repo of the same Project (e.g. a frontend task depending on a backend task in a sibling repo) - this is the actual point of doing Project-scoped ingestion instead of repo-scoped. Ingestion never crosses Project boundaries.
- (decision) Seam 2 resolved: CAP-5's filter/search operates on card identity (Project name / repo name) generically at the board-rendering layer, so it applies uniformly to every card type rendered on a board or overview - both CAP-2 Project cards and CAP-10 TaskLink cards - with no separate filtering logic for recipe cards.
- (decision) Seam 3 resolved: architecture for CAP-8-11 written as AD-8 (recipe schema widening lives in the existing sidecar validation function, additive branch, no new table), AD-9 (TaskLinkRow owned by new recipe_service.py, FK to project_id and nullable job_id, ingestion is a stateless parser invoked on-demand, never a background watcher), AD-10 (spawn_task reuses the existing job-creation service function directly - same worktree/branch provisioning path as a user-initiated job, not a new execution pipeline), AD-11 (TaskLink read model exposed via a new GET /settings/projects/{id}/task-links endpoint, separate from GET /settings/projects/summary, polled by the board the same way jobs are, so recipe cards and job cards share one rendering pass client-side).
- (decision) Seam 4 resolved: ui-flows.md gets a new CAP-8-11 section with a wireframe showing a chained TaskLink card (dependency badge, chained-lifetime indicator, greyed-out until dependencies satisfied) sitting in the same column grid as regular job cards on the CAP-1 board - not a separate screen. Mockup gets an example of this on the board screen.
- (note) All four integration seams rendered: SPEC.md CAP-9/CAP-5 rewritten for Project-scoped ingestion and uniform filtering; ARCHITECTURE-SPINE.md gained AD-8-11, updated frontmatter (binds now CAP-1-11), mermaid diagram, structural seed, capability map; ui-flows.md gained a CAP-8-11 section with a cross-repo dependency wireframe; mockup's board screen now shows a live chained recipe card and a greyed-out waiting-on-dependency card with visible styling. Spec, architecture, and both companions are now consistently unified across CAP-1-11, not just renumbered.
- (note) Grep audit of ripple surfaces beyond CAP-1-11's direct scope: (1) MCP surface - codeplane_job create takes a bare repo param, codeplane_repo tool is list/get/register/remove with no project concept at all, no codeplane_project tool exists. (2) unregister_repo_endpoint (DELETE /settings/repos/{path}) has zero guard today against active jobs OR ProjectRow membership - removing a repo that's a member of an explicit Project would leave a dangling repo_paths entry. (3) PlatformConfig.repos binds auth to a repo list - a pre-existing, unrelated grouping axis (auth, not organization) that must stay orthogonal to Project and not be confused with it. (4) cleanup_worktrees, analytics.py's cost_by_repo/analytics_repos, and RepoHealth/RepoCost are all repo-keyed today, not Project-aware - CAP-2/3 need a Project-level rollup on top without breaking these. (5) DashboardScreen/KanbanBoard's cross-repo flat board still exists as-is post-CAP-1-4 - its fate (kept as-is, redirected, or deprecated) was never decided.
- (decision) Ripple decisions: (a) MCP gains a new codeplane_project tool (list/get/create/update/assign-repos/attach-tracker-link/ingest-tasks), additive alongside the unchanged codeplane_repo and codeplane_job tools - Job creation stays repo-scoped (a Job always targets one repo; Project is a read/organization layer above it, never a job-creation parameter). (b) unregister_repo gains a guard: blocked while the repo is a member of an explicit ProjectRow (must be reassigned/removed from the Project first), mirroring the existing pattern of blocking destructive action while referenced elsewhere (same shape as AD-6's Credential-deletion guard). (c) PlatformConfig.repos is explicitly out of scope and orthogonal - documented as a non-goal so it's never conflated with Project membership. (d) Analytics/cost/health stay repo-keyed as the source of truth; CAP-2/3's Project rollup is computed by summing the existing per-repo values for a Project's member repos, not a second parallel aggregation pipeline. (e) DashboardScreen/KanbanBoard (the old flat cross-repo board) is kept as-is, unchanged, not deprecated or redirected - it remains available for anyone who wants the old flat view, while /repos becomes the primary Project-organized entry point.
- (decision) Zero-user reframe: DashboardScreen/KanbanBoard flat cross-repo view is retired, not kept alongside. /repos (Projects Overview + per-Project board) becomes the single entry point. No synthetic 'default/All' Project entity - the Overview grid plus CAP-3's rolled-up attention count already serves as the all-up glance; adding a catch-all Project would be a redundant second way to see the same data.
- (capability) CAP-12 (new): Project-scoped lightweight Chat, distinct from Job. New ChatRow (id, project_id, title, created_at, last_message_at, status). A Chat has no worktree, no branch, no git operations at all - purely conversational, optionally with read-only repo context via existing workspace read tools, never commit-capable. Verified existing plan-mode flow (test_plan_mode_flow.py) is still Job-scoped (worktree_path already set at job creation) - confirms no lightweight pre-Job unit exists today. A Chat can be 'promoted' to a real Job at any point, seeding the new Job's initial prompt/context from the chat transcript and provisioning a fresh worktree/branch only at that moment - this is the actual bridge from 'just talking' to 'building it', mirroring the Copilot app's own chat-to-session pattern.
- (capability) CAP-13 (new): Orchestrator session, scoped to a Project or a specific TaskLink dependency chain, distinct from both a Chat (CAP-12) and a sidecar (existing SecondarySessionRow, job-scoped). New OrchestratorSessionRow (id, project_id, scope: 'project' or a specific chain root task_link_id, status). It observes the TaskLink graph and every Job spawned from it, posts narration/summaries, and - critically - can gate the CAP-10 auto-spawn step behind a hold: instead of a completed TaskLink immediately auto-spawning its dependent, an active OrchestratorSessionRow can require explicit confirmation first, routed through the existing codeplane_approval flow exactly like CAP-7/CAP-11's writes. It does not execute code itself and is not a new job-execution entity - it is a supervisory read+narrate+gate layer over the chain, closer to a human watching a cline/kanban board than to an agent doing the work.
- (constraint) CAP-12/CAP-13 constraints: Chat never provisions git state until promoted to a Job (GitService isolation preserved - a Chat literally cannot call GitService). OrchestratorSessionRow's spawn-gating is opt-in per chain (default is CAP-10's existing auto-spawn-on-completion; enabling an orchestrator for a chain switches it to gated) - CAP-10's ungated default behavior for chains with no orchestrator is unchanged, additive only.
- (note) Rendered ripple-effects thread into SPEC.md (Why/Non-goals/Success signal updated for CAP-12/13, DashboardScreen retirement, PlatformConfig orthogonality) and ARCHITECTURE-SPINE.md (AD-12 Chat, AD-13 Orchestrator, frontmatter binds extended to CAP-13, structural seed adds chat_service.py/chats.py/ChatPanel.tsx/OrchestratorPanel.tsx/mcp codeplane_project tool, KanbanBoard/DashboardScreen marked retired in structural seed, capability map extended).
- (note) Rendered CAP-12/13 into ui-flows.md (new sections: Chats flat-list tab wireframe, Orchestrator narration/gated-card wireframe) and into the mockup (project-boards-mockup.html): new Chats tab/screen with promote buttons, held/gated recipe card + orchestrator approval strip on the board. Ripple-effects rendering across SPEC.md/ARCHITECTURE-SPINE.md/ui-flows.md/mockup is now complete and consistent.
- (decision) REVISED CAP-12, CAP-13 removed as separate: user challenged why Chat was hard Project-scoped and rejected the 'Chat promotes into a Job' transform model outright ('chats dont become tasks... this idea of chats becoming tasks is wrong'). Redesigned: Chat and Orchestrator merge into ONE entity (ChatRow). A Chat is a persistent, git-free conversation that (a) can LAUNCH one or more Jobs directly from itself over time without being consumed/transformed - the chat keeps existing after launching a Job - and (b) can attach to a Task Recipe chain to narrate/gate its auto-spawn (the old CAP-13 Orchestrator behavior), as an action the same entity can take, not a separate kind of session. ChatRow.project_id becomes NULLABLE: creation-time default is context-driven (created from inside a Project board -> defaults to that Project; created from global/nav -> unscoped/null), always user-overridable at creation. An unscoped Chat's project_id is settled the first time it actually takes an action requiring one (launching a Job into a specific repo, or attaching to gate a specific chain's TaskLink) - prompted at that moment, not before. No 'promotion' terminology or transform semantics remain. Old separate CAP-13 (Orchestrator) is retired/merged into revised CAP-12; AD-13 likewise merges into a revised AD-12.
- (decision) CORRECTED CAP-6: user rejected the 'implicit 1:1 Project wrap of an orphan registered repo' framing outright - 'users cannot add repos. they can add projects and repos can only be added to projects or as projects.' This is not additive backward-compat, it's a replacement of the add-flow: today's standalone register_repo_endpoint/create_repo_endpoint (verified in backend/api/settings.py lines 394/440, a bare 'add a repo' action with no Project concept) is retired. The only add action going forward is 'add a Project': the creation dialog always creates a ProjectRow, and a repo path is supplied either as that Project's sole repo (the common single-repo case, functionally equivalent in shape to today's single-repo card but always backed by a real ProjectRow, never a repo-only row with an implicit wrap at read-time) or added into an existing multi-repo Project. There is no code path that creates a bare, Project-less repo entry. This changes CAP-4 (empty states)/CAP-5/CAP-6 wording and AD-5's 'implicit wrap' framing, and changes the register_repo/create_repo backend endpoints from repo-scoped to Project-scoped (still ultimately calling the existing repo-clone/register logic, just always inside a create-Project transaction).
- (note) Rendered the no-bare-repo correction into SPEC.md (Why, CAP-4/CAP-6 rewritten to remove implicit-wrap language, MCP constraint rewritten: codeplane_project create/update is the only add-path, codeplane_repo narrows to read-only list/get, unregister_repo constraint replaced) and ARCHITECTURE-SPINE.md (AD-5 rewritten: ProjectRow is the sole repo-membership entity, no read-time wrap; AD-3 updated to GET /settings/projects/summary; structural seed/cap-map updated). ui-flows.md and the mockup did not reference implicit-wrap language directly and did not need changes, verified by grep (no 'wrap' or 'register' matches outside already-correct Project-creation framing).
- (note) Fixed remaining 'auto-wrapped'/'registered repo' leftovers in ui-flows.md (screen-map intro paragraph, data-flow paragraph). Mockup verified clean via grep - it never depicted a standalone repo-add flow, no changes needed. The no-bare-repo correction is now fully consistent across SPEC.md, ARCHITECTURE-SPINE.md, and ui-flows.md.
- (note) Continued the BMAD pipeline per user request 'keep tracking through the bmad pipeline to get actionable tasks'. Note: bmad-create-epics-and-stories has no installed skill package in this checkout (_bmad/bmm only contains config.yaml/module-help.csv catalog metadata, no runnable content), same situation as bmad-architecture earlier. Hand-authored planning-artifacts/epics-stories/epics-stories-project-boards/EPICS-AND-STORIES.md in the BMAD epics/stories shape, deriving 6 epics / 21 stories directly and only from SPEC.md's CAP-1-12 and ARCHITECTURE-SPINE.md's AD-1-12 (dependency order: Epic 1 Project entity -> Epics 2/3/4 in parallel -> Epic 5 recipes -> Epic 6 Chat). No new product decisions were made in this step, pure derivation.
- (capability) Added CAP-13 (unified codeplane_tracker MCP tool: comment/transition on a linked tracker ticket, agent-callable mid-job, routes through the existing codeplane_approval gate) and CAP-14 (unified codeplane_pr MCP tool: agent-callable mid-job PR request, alongside the existing automatic backend-triggered PR creation at completion/merge time). Surfaced during epics-and-stories requirements extraction: user flagged that PR creation today is backend-owned/invisible to the agent (merge_service._create_pr, triggered by merge strategy at completion, verified via grep), and asked whether tracker writes and PR creation should instead expose a first-class agent-facing MCP tool rather than relying on backend-only automation or the agent shelling raw CLI/API calls itself. User chose: tracker writes get a new unified MCP tool gated by approval (CAP-13); PR creation gets an additional agent-callable MCP tool alongside (not replacing) the existing automatic backend path (CAP-14).
- (decision) Added NFR9: PAT scope guidance is per-provider, not uniform. GitHub fine-grained PATs can be scoped per-repo (Issues R/W for tracker; Contents+Pull requests R/W for PR creation) so Credential blast radius can match a TrackerLink's declared repos. Jira API-token auth (basic auth) has no granular scope at all -- token inherits the full account's permissions; OAuth 2.0 3LO would allow read:jira-work/write:jira-work but NFR3 already rules out OAuth app registrations, so Jira Credentials are inherently unscoped. Azure DevOps PATs are org-scoped not project-scoped (Work Items R/W for tracker, Code R/W for PR path) -- one token covers every project in that org even though a TrackerLink only references one. Consequence: for Jira and AzDO, the codeplane_approval write-back gate is the real security boundary, not token scope; for GitHub, CodePlane should validate/warn if a registered Credential's actual scope is broader than the repos its TrackerLinks reference. Settings > Integrations UI should surface a copy-paste minimal-scope hint per provider at Credential creation time.
- (correction) Retracted the 'validate/warn if GitHub Credential scope is broader than its TrackerLinks' repos' clause from NFR9. Credential is deliberately global/shared by design (register once, attach to any number of Projects over time) -- a broader-than-any-single-TrackerLink scope is the expected steady state, not an anomaly, so a warning would fire constantly and contradict the model instead of catching a real problem. Kept only the per-provider scope documentation / copy-paste hint at Credential-creation time; removed the runtime validate/warn behavior entirely.
- (correction) Trimmed trailing clause from NFR9 in SPEC.md ('this must not be represented as a solved problem in the Integrations UI') -- unwarranted defensiveness against a strawman nobody proposed. epics.md's NFR9 phrasing already lacked this clause, no change needed there.
- (decision) bmad-create-epics-and-stories step-02 complete and approved: consolidated the initial 7-epic sketch down to 5 epics, merging the two technical-layer-only epics (Project entity, sidecar schema widening) into the epics that deliver their actual user value, per the step's own anti-pattern rule. Final: Epic1 Project-based organization (FR1-6,NFR5,NFR6,UX-DR1,UX-DR2), Epic2 Tracker integration (FR7,NFR1-3,NFR9,UX-DR3), Epic3 Task Recipe chaining (FR8-11,UX-DR4), Epic4 Chat (FR12,NFR8,UX-DR5), Epic5 Agent-facing MCP tools (FR13,FR14). NFR2/4/7 cross-cutting. epics.md updated with FR coverage map + epic list, stepsCompleted now [1,2]. Proceeding to step-03-create-stories.md.
- (decision) step-03: Epic 1 (Project-based organization) stories approved and appended to epics.md -- 5 stories (1.1 Create/Edit a Project, 1.2 View Projects Overview, 1.3 View a Project's board, 1.4 Cross-Project attention signal, 1.5 Filter by name), Given/When/Then ACs written, all of FR1-6/NFR5/NFR6/UX-DR1/UX-DR2 covered. Proceeding to Epic 2 (Tracker integration) story breakdown.
- (decision) step-03: Epic 2 (Tracker integration) stories approved and appended to epics.md -- 5 stories (2.1 Register a Credential, 2.2 Attach a TrackerLink, 2.3 View synced ticket state, 2.4 Approve a tracker write-back, 2.5 See per-provider PAT scope guidance), all of FR7/NFR1/NFR2/NFR3/NFR9/UX-DR3 covered. Proceeding to Epic 3 (Task Recipe chaining) story breakdown.
- (decision) Amended CAP-9/AD-9: TaskLinkRow.story_node_id becomes nullable; added tracker_ticket_ref (nullable) and prompt_override (nullable). Two independent, non-coercive creation paths: (a) ingestion from BMAD/spec-kit as before, populates story_node_id only; (b) new manual-assignment path, targets an existing external ticket already visible via a Project's TrackerLink, populates tracker_ticket_ref + prompt_override, no story_node_id. Many TaskLinks may share one tracker_ticket_ref. Either field may be populated on the other path's row after the fact; exactly one of story_node_id/tracker_ticket_ref is guaranteed non-null at creation, never neither. CAP-11's tracker_write now targets the firing TaskLink's own tracker_ticket_ref specifically -- no fallback to an ambiguous Project-level default ticket. Clarified AD-13 (agent-facing codeplane_tracker) operates at Project-TrackerLink granularity, a level above this per-TaskLink targeting -- intentionally different, not inconsistent. Updated SPEC.md CAP-9 intent/success + new Constraints bullet, ARCHITECTURE-SPINE.md AD-9 + AD-13 rule text, epics.md FR9/FR11 wording. This was in response to user's design question about whether external tracker stories and Task Recipe chain nodes are meant to be the same object -- answer: loosely, optionally coupled, not merged 1:1.
- (decision) step-03: Epic 3 (Task Recipe chaining) stories approved and appended to epics.md -- 6 stories (was 5, split into 3.2 ingest / 3.3 manual-assign-to-ticket after the CAP-9/AD-9 dual-creation-path amendment): 3.1 Widen the Task Recipe vocabulary, 3.2 Ingest a task graph into a Project, 3.3 Manually assign a task to an existing ticket, 3.4 See TaskLink cards on the board, 3.5 Auto-spawn the next task on completion, 3.6 Route recipe tracker-writes to the paired ticket. All of FR8/FR9/FR10/FR11/UX-DR4 covered. Proceeding to Epic 4 (Chat) story breakdown.
- (decision) step-03: Epic 4 (Chat) stories approved and appended to epics.md -- 4 stories (4.1 Start a Chat, 4.2 Launch a Job from a Chat, 4.3 Attach a Chat to a Task Recipe chain, 4.4 Gate a chain's auto-spawn behind approval), all of FR12/NFR8/UX-DR5 covered. Proceeding to Epic 5 (Agent-facing MCP tools), the final epic.
- (decision) step-03 COMPLETE: all 5 epics, 23 stories total (1: 5 stories, 2: 5, 3: 6, 4: 4, 5: 2) generated and appended to epics.md with Given/When/Then ACs. All FR1-14/NFR1-9/UX-DR1-5 covered across stories. stepsCompleted now [1,2,3]. Proceeding to step-04-final-validation.md.
- (decision) Added epic_id (nullable) to TaskLinkRow (CAP-9/AD-9): sourced from the ingested BMAD story's Epic membership when available (null for manually-assigned/tracker-ticket-only TaskLinks, since those have no Epic origin). Purely cosmetic/grouping field, no new behavior on its own. Amended CAP-12/AD-12 (Chat attach-chain): when a Chat attaches to a chain and every TaskLink in that connected chain shares the same non-null epic_id, the board labels that Chat's card as supervising that Epic; if the chain spans multiple epics or contains any TaskLink with a null epic_id, it renders generically as 'chain', never mislabeled as an Epic. No new entity, no new attach-flow -- reuses the existing Chat/chain-attach mechanism, epic_id is just truth-in-advertising label data. Rationale: a dependency-graph 'chain' and a BMAD 'Epic' are not guaranteed to coincide (chain boundaries come from depends_on edges, not epic tags), so unconditional Epic-labeling would be a correctness bug; gating the label on shared epic_id keeps it honest.
- (correction) Relocated ARCHITECTURE-SPINE.md (+ its .memlog.md) from the incorrect ad-hoc path spec-project-boards/architecture/ to its real bmad-architecture skill output path: _bmad-output/planning-artifacts/architecture/architecture-codeplane-2026-08-10/ARCHITECTURE-SPINE.md. Root cause: per bmad-spec/SKILL.md's own documented rule, architecture content is an ADOPTED companion (authored by the separate upstream bmad-architecture skill, referenced by relative path, never nested inside the spec folder) -- not a spec-authored companion. ui-flows.md correctly stays as a sibling of SPEC.md, since it is a spec-authored, diagram-bearing companion per the same rule, and needed no change. SPEC.md's companions: frontmatter and epics.md's inputDocuments: frontmatter updated to the corrected relative/absolute paths. No content was altered -- ARCHITECTURE-SPINE.md's frontmatter already matched spine-template.md's real shape, confirming this was a placement error, not a fabrication. Flagged by the user directly challenging the ad-hoc path during bmad-check-implementation-readiness step-01 document discovery.
- (correction) Story count was miscounted as 23 in the step-03-COMPLETE entry and propagated into the readiness report; actual count per epic (5+5+6+4+2) is 22, confirmed by counting '#### Story' headers in epics.md directly during bmad-sprint-planning's story extraction. Fixed in implementation-readiness-report-2026-08-10.md; this memlog entry corrects the record without editing the original (append-only).
