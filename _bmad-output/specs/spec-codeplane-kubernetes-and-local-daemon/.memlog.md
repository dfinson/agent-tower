---
topic: CodePlane Kubernetes-native and local-daemon deployment modes
updated: 2026-08-05T16:48
---

- (direction) Headless express run explicitly approved; slug is codeplane-kubernetes-and-local-daemon; write only within the target spec folder and do not implement, commit, push, or alter project files.
- (direction) Treat README.md, repository SPEC.md, and relevant docs as brownfield source context; preserve every deployment-relevant existing product guarantee.
- (decision) The product has two equally first-class modes: Kubernetes-native and local-daemon; neither is a fallback for the other.
- (decision) Shared product semantics are mode-independent; mode-specific mechanics and intentional differences must be explicit and testable.
- (decision) Architectural choices not established by the intent or brownfield sources remain open questions.
- (capability) CAP-1 [common] Operators can choose Kubernetes-native or local-daemon deployment as a fully supported product mode; success: release documentation and acceptance tests identify both modes as supported and neither requires the other or a hosted control plane.
- (capability) CAP-2 [common] Operators receive the same core job, session, state, approval, intervention, review, resolution, artifact, analytics, and audit semantics in both modes; success: a cross-mode conformance suite passes the shared lifecycle and API behavior matrix, with every exception documented as an intentional difference.
- (capability) CAP-3 [common] Operators can register, access, and isolate repositories and job workspaces without agents modifying the primary checkout; success: each managed job uses an isolated workspace and concurrent jobs cannot mutate one another or the protected primary checkout in either mode.
- (capability) CAP-4 [common] Operators can select where agent execution occurs relative to the control plane and repository boundary; success: every job reports its execution locality and rejects execution when required repository, credential, or policy boundaries cannot be satisfied.
- (capability) CAP-5 [common] Job records, canonical events, approvals, transcripts, telemetry, trails, artifacts, and workspace outcomes remain durable according to the selected mode policy; success: declared durable data survives the mode-specific restart, upgrade, and recovery tests without silent loss or reordering.
- (capability) CAP-6 [common] Operators can configure global, repository, and job behavior and supply secrets without exposing secret values through APIs, events, logs, artifacts, or UI; success: precedence and secret-redaction tests pass in both modes.
- (capability) CAP-7 [common] Every command, query, approval, and agent action is evaluated against an explicit identity, authorization, and policy context appropriate to the mode; success: unauthorized cross-boundary access and approval attempts are denied and audited.
- (capability) CAP-8 [common] Operators can define and observe job concurrency, queueing, cancellation, and fairness limits; success: deterministic capacity tests show jobs queue, start, cancel, and recover without exceeding configured limits.
- (capability) CAP-9 [common] Clients receive ordered live events and can reconnect to a consistent current state after interruption; success: disconnect, replay-window, snapshot, duplicate, and out-of-order tests converge the client to authoritative state in both modes.
- (capability) CAP-10 [common] Operators can access CodePlane locally or remotely through an authenticated ingress appropriate to the mode; success: supported access paths provide health, UI, REST, SSE, MCP, and applicable interactive channels while unauthenticated paths remain limited to explicitly public surfaces.
- (capability) CAP-11 [common] Operators can diagnose service, job, agent, repository, storage, and event-stream health using correlated logs, metrics, traces, events, and artifacts; success: a failed-job and degraded-service exercise identifies the affected job, component, cause, and recovery action without direct database inspection.
- (capability) CAP-12 [common] CodePlane detects interrupted work and recovers or terminates it according to documented mode-specific guarantees; success: process, node, network, storage, and agent interruption tests produce explicit states with no orphaned approvals or silently running agents.
- (capability) CAP-13 [common] Operators can install, upgrade, downgrade where supported, and remove each mode with documented compatibility and data-protection behavior; success: supported lifecycle tests preserve compatible configuration and durable data and fail safely on incompatible transitions.
- (capability) CAP-14 [common] Operators can move or interoperate selected repositories, configuration, policy, history, and artifacts between modes without changing shared identifiers or semantics unexpectedly; success: an export/import or bridge demonstration preserves the declared portable dataset and reports every non-portable item.
- (capability) CAP-15 [common] Each deployment exposes clear trust, filesystem, network, credential, secret, and agent-execution boundaries; success: threat-model acceptance tests prove repository containment, secret non-disclosure, authenticated mutation, and approval-policy enforcement.
- (capability) CAP-16 [kubernetes] Cluster operators can deploy and operate a Kubernetes-native CodePlane instance using supported Kubernetes packaging and lifecycle procedures; success: a clean supported cluster installation reaches ready state, serves the product surfaces, and completes a managed job.
- (capability) CAP-17 [kubernetes] Kubernetes operators can make repositories and isolated workspaces available to agents through a declared access model; success: private and public repository scenarios complete clone or attachment, concurrent workspace isolation, change review, and resolution without undeclared host filesystem dependence.
- (capability) CAP-18 [kubernetes] Kubernetes-native scheduling can place and limit control-plane and agent workloads across cluster resources; success: configured quotas, pending capacity, cancellation, and workload cleanup behave predictably under concurrent load.
- (capability) CAP-19 [kubernetes] Kubernetes-native durable state and artifacts survive pod replacement and supported upgrades to the declared durability objective; success: forced pod replacement during representative workflows preserves or explicitly recovers all guaranteed data.
- (capability) CAP-20 [kubernetes] Kubernetes-native identity, tenancy, authorization, and approval delegation support the declared organizational model; success: tenant and role isolation tests prevent unauthorized job, repository, artifact, secret, terminal, MCP, and approval access.
- (capability) CAP-21 [kubernetes] Kubernetes-native ingress, service health, observability, scaling, and availability integrate with supported cluster operations; success: readiness, liveness, rollout, load, and telemetry demonstrations meet the declared service objectives without changing shared product semantics.
- (capability) CAP-22 [local-daemon] A developer can install CodePlane and use cpl setup, cpl doctor, cpl up, cpl down, cpl restart, cpl info, and cpl version on a workstation; success: the supported local installation path starts a localhost service and retains existing CLI lifecycle behavior.
- (capability) CAP-23 [local-daemon] A developer can use local repositories, Git worktrees, locally authenticated agent CLIs, native CLI-session mirroring, local terminals, and existing merge, PR, or discard workflows; success: managed and mirrored sessions complete end to end against local repositories without a cluster or central service.
- (capability) CAP-24 [local-daemon] A single user can operate with local SQLite data, local artifacts and credentials, optional Dev Tunnel or Cloudflare remote access, and offline or local-first behavior; success: core local workflows function with external network access disabled except where the chosen agent or explicit remote operation inherently requires it.
- (constraint) Local-daemon mode remains a supported product mode, not compatibility, legacy, development-only, or degraded operation.
- (constraint) Kubernetes-native mode must not redefine CodePlane as Kubernetes-only, cloud-only, or dependent on a vendor-hosted or central CodePlane service.
- (constraint) Existing local installation, cpl up lifecycle, local repository registration, Git worktree isolation, native CLI-session mirroring, SQLite single-user operation, local credentials, tunnels, and offline/local-first behavior are preserved.
- (constraint) Shared job states, canonical event meaning, approvals, operator intervention, review and resolution semantics, API contracts, and audit provenance remain consistent unless an intentional mode difference is named and validated.
- (constraint) The specification states product requirements and observable guarantees without selecting Kubernetes controllers, databases, queues, storage classes, workload primitives, package managers, identity providers, or network products.
- (constraint) Mode-specific security must preserve least privilege, repository and workspace containment, authenticated mutation, policy enforcement, secret non-disclosure, and auditable approvals.
- (constraint) Deployment and upgrade procedures must protect durable state and expose incompatibility before destructive changes.
- (constraint) Repository and agent credentials remain under operator control and are not copied to a central hosted service.
- (constraint) All source changes, dependencies, BMAD configuration, existing documentation, and artifacts outside this spec folder are out of bounds for this run.
- (decision) Companion brownfield-constraints.md holds the preserved local product baseline and source-to-requirement traceability.
- (decision) Companion mode-requirements.md holds common versus mode-specific requirement matrices and intentional differences.
- (assumption) The current documented local-daemon behavior is the compatibility baseline unless a future source decision explicitly supersedes it.
- (assumption) Kubernetes-native operation may serve more than one human or team, but the required tenancy model and scale are not yet decided.
- (assumption) Core product semantics can be defined independently from the eventual Kubernetes implementation architecture.
- (question) What Kubernetes tenancy boundary is required: per user, team, namespace, cluster, repository, or another model?
- (question) Which identity providers, roles, authorization rules, service identities, and approval-delegation rules must Kubernetes-native mode support?
- (question) Where may agents execute in Kubernetes-native mode, and what isolation, sandboxing, privilege, network, and resource boundaries are required?
- (question) How are local, private, large, or already-cloned repositories made available to Kubernetes-native jobs, and which workspace lifecycle is portable between modes?
- (question) Which data is durable in Kubernetes-native mode, what consistency and backup or restore guarantees apply, and what are the retention defaults?
- (question) What ordering, delivery, replay, and backpressure guarantees must the Kubernetes event path provide at target scale?
- (question) What supported concurrency, throughput, latency, availability, recovery-time, and recovery-point objectives define Kubernetes-native readiness?
- (question) Which Kubernetes versions, distributions, CPU architectures, disconnected environments, and packaging or upgrade mechanisms are supported?
- (question) What ingress, TLS, DNS, WebSocket or terminal, SSE, MCP, port-preview, and network-policy behaviors are required in Kubernetes-native mode?
- (question) Which secret stores and credential injection or rotation workflows must Kubernetes-native mode integrate with?
- (question) Does native CLI-session mirroring remain local-daemon-only, or must Kubernetes-native mode ingest sessions running on developer machines or remote workers?
- (question) What configuration, policy, repository metadata, history, artifacts, and identifiers are portable between modes, and may both modes coordinate the same repository concurrently?
- (question) What downgrade, rollback, schema compatibility, and mixed-version behavior is required during upgrades?
- (question) Which observability formats and integrations are mandatory beyond CodePlane internal logs, metrics, traces, events, and artifacts?
- (question) Which local-only features require Kubernetes analogues, intentional omission, or revised semantics, including PTY terminals, local voice transcription, tunnels, push notifications, sharing, and localhost port preview?
- (decision) Non-goals include choosing Kubernetes architecture, deprecating local mode, requiring a hosted service, changing agent providers, rewriting core product semantics, or implementing this specification.
- (note) Wrapper-only content dropped: unattended approval, headless invocation wording, no-commit instructions, and requested return formatting affect workflow execution but not the product contract.
- (decision) WHY: CodePlane must extend from its local-first single-developer foundation into Kubernetes-native environments without sacrificing the workstation product that existing users rely on; the opportunity is broader team and cluster operation while preserving one coherent control-plane product.
- (decision) SUCCESS: A release candidate passes the shared cross-mode conformance suite, a clean Kubernetes deployment completes and recovers a representative managed workflow, and an offline local-daemon installation completes managed and mirrored workflows with no cluster or hosted CodePlane dependency.
- (note) SOURCE: ../../../SPEC.md
- (note) SOURCE: ../../../README.md
- (note) SOURCE: ../../../docs/architecture.md
- (note) SOURCE: ../../../docs/configuration.md
- (note) SOURCE: ../../../docs/guide.md
- (note) SOURCE: ../../../docs/quick-start.md
- (note) SOURCE: ../../../docs/security.md
- (note) SOURCE: ../../../docs/reference/cli.md
- (note) SOURCE: ../../../docs/reference/job-states.md
- (note) SOURCE: ../../../docs/reference/sse-events.md
- (note) BROWNFIELD: Product identity | CodePlane is a control plane that runs, observes, supervises, and reviews coding-agent work; it is not itself an agent model.
- (note) BROWNFIELD: Local lifecycle | Python package installation and cpl setup, doctor, up, down, restart, info, and version form the workstation lifecycle.
- (note) BROWNFIELD: Local data | Single-user state, canonical events, telemetry, trails, and metadata use local SQLite under ~/.codeplane; artifacts, config, logs, and VAPID material are local assets.
- (note) BROWNFIELD: Repository safety | Registered local or cloned Git repositories are allowlisted; managed jobs use isolated Git worktrees and do not execute in the primary checkout.
- (note) BROWNFIELD: Agent integration | Existing locally installed and authenticated Copilot and Claude CLIs or SDK adapters supply execution; credentials remain local and adapter boundaries normalize sessions.
- (note) BROWNFIELD: Native mirroring | Native Copilot and Claude CLI sessions can be discovered and ingested without requiring users to launch work through CodePlane.
- (note) BROWNFIELD: Events and state | Canonical domain events drive persistence, state transitions, live SSE, replay, snapshots, approvals, and audit history; the documented job state machine is authoritative.
- (note) BROWNFIELD: Remote access | Localhost is the safe default; optional Dev Tunnel or Cloudflare paths add authenticated remote and mobile access without converting CodePlane into a hosted service.
- (note) BROWNFIELD: Offline posture | Product data and local transcription remain on the workstation by default; network use is explicit for agent providers, Git remotes, tunnels, OTEL export, and other configured integrations.
- (note) BROWNFIELD: Operator control | Policy evaluation, hard gates, approvals, intervention, cancellation, review, merge or PR or discard resolution, and provenance remain core semantics.
- (note) AREA: Deployment and upgrade lifecycle | Both modes expose install, readiness, upgrade, rollback or safe failure, and removal contracts | Cluster packaging, rollout, compatibility, and data-safe upgrade behavior are required but mechanism is open | Existing cpl and package lifecycle remains supported.
- (note) AREA: Repository and workspace access | Registered repositories are bounded and every managed job is isolated | Repository acquisition, attachment, caching, and workspace provisioning are open architectural choices | Absolute local paths, local clones, existing credentials, and Git worktrees remain supported.
- (note) AREA: Agent execution locality | Every job declares execution locality and enforces workspace, credential, policy, and resource boundaries | In-cluster, remote-worker, or hybrid execution remains open | Agents run on the workstation and native local CLI sessions remain mirrorable.
- (note) AREA: Persistence and artifact durability | Declared durable records preserve canonical identifiers, ordering, provenance, and retention semantics | Store topology, consistency, backups, HA, and artifact backend remain open | SQLite and local filesystem assets remain the supported single-user implementation.
- (note) AREA: Configuration and secrets | Global, repository, and job precedence is explicit and secrets never enter logs, events, artifacts, or UI | Secret-store integrations, rotation, and namespace scope remain open | Local config, environment variables, CLI credential stores, and repository config remain supported.
- (note) AREA: Identity tenancy authorization approvals | Mutation and approvals require an auditable authorized identity and policy context | Tenant boundaries, roles, IdPs, service identities, and delegated approvals remain open | Single-user localhost trust and authenticated remote access remain supported.
- (note) AREA: Scheduling and concurrency | Capacity, queueing, fairness, cancellation, cleanup, and limits are observable and deterministic | Cluster scheduling, quotas, priorities, and workload placement remain open | RuntimeService-style local concurrency limits and queues remain supported semantics.
- (note) AREA: Event streaming | Canonical events, ordered client convergence, bounded replay, snapshots, and job-scoped streams are shared | Distributed transport, delivery, partitioning, and backpressure remain open | In-process publication, SQLite replay, and SSE remain supported.
- (note) AREA: Ingress and remote access | UI, REST, SSE, MCP, and applicable interactive channels share authenticated access rules | Ingress, TLS, DNS, network policy, terminal, preview, and public sharing integration remain open | Localhost plus optional Dev Tunnel or Cloudflare access remains supported.
- (note) AREA: Observability | Correlation across service, job, session, event, repository, artifact, cost, and failure data is shared | Cluster-native telemetry integrations and retention remain open | Structured local logs, health, artifacts, analytics, and optional OTEL export remain supported.
- (note) AREA: Failure recovery | Interruptions yield explicit states, durable evidence, bounded recovery, and no orphaned approvals or agents | Pod, node, network, storage, and partial-service recovery objectives remain open | Existing graceful shutdown, restart recovery, rerun, replay, and heartbeat semantics remain the baseline.
- (note) AREA: Scaling and availability | Declared service objectives and degradation behavior are testable | Horizontal scale, HA, leader coordination, throughput, RTO, and RPO remain open | Single-user and workstation-scale limits remain valid rather than being judged against cluster HA goals.
- (note) AREA: Security boundaries | Least privilege, repository containment, secret protection, authenticated mutation, policy enforcement, and auditability are invariant | Pod or workload isolation, service accounts, network boundaries, tenant isolation, and cluster privileges remain open | OS-user permissions, worktree containment, localhost trust, password and tunnel gates, and local credentials remain supported.
- (note) AREA: Packaging | Each mode has a supported, diagnosable distribution with version and compatibility information | Kubernetes packaging format and supported distributions remain open | Python package and cpl entry point remain supported.
- (note) AREA: Migration and interoperability | Portable data is explicit, identifiers and semantics are preserved, and non-portable data is reported | Import, export, bridging, and mixed-mode coordination remain open | Local operation never requires migration to Kubernetes and remains independently complete.
- (note) DIFFERENCE: Local-daemon may trust same-machine localhost and use one OS user, while Kubernetes-native requires an explicit network identity, tenancy, and authorization contract.
- (note) DIFFERENCE: Local-daemon may use workstation paths, processes, SQLite, and local filesystem durability, while Kubernetes-native must expose equivalent product outcomes through cluster-appropriate boundaries without prescribing the implementation.
- (note) DIFFERENCE: Native CLI-session mirroring, local PTYs, local transcription, tunnels, push, sharing, and port previews remain guaranteed locally; Kubernetes parity or intentional omission for each is unresolved.
- (note) DIFFERENCE: Availability and scale objectives are mode-specific; local single-user constraints are valid product characteristics, not failures to meet Kubernetes service levels.
- (non-goal) Selecting Kubernetes workload, controller, database, queue, storage, ingress, identity, secret, or packaging implementations.
- (non-goal) Deprecating, subordinating, or forcing migration away from local-daemon mode.
- (non-goal) Making CodePlane cloud-only or dependent on a central hosted CodePlane service.
- (non-goal) Changing supported agent providers or redesigning job, event, approval, review, and resolution semantics unrelated to deployment mode.
- (non-goal) Implementing Kubernetes support, modifying source code, or producing an implementation architecture in this specification run.
- (question) Which source is authoritative where current SPEC.md and docs disagree on policy defaults, restart or session recovery, heartbeat cadence, and SSE event naming, and must conformance preserve deployed behavior or the root specification?
- (note) SOURCE-CONFLICT: Root SPEC.md and user docs disagree on policy preset defaults, restart recovery, heartbeat cadence, and SSE vocabulary; no choice was invented and the authority decision remains open.
- (event) Coherence validation PASS: Spec Law rules 1-6 and 8 satisfied; 24 unique capabilities each have WHAT intent and testable success, with explicit constraints, non-goals, assumptions, questions, and concrete release signals.
- (event) Preservation validation PASS: deployment-relevant claims from README.md, SPEC.md, and docs are represented in the kernel or companions; known live-source conflicts are preserved as an open question; wrapper-only instructions are logged.
- (event) Specification finalized through memlog derivation in headless mode.
- (decision) The default action-policy preset is supervised; existing runtime behavior and user documentation are authoritative, and stale root SPEC.md passages naming autonomous as the default are superseded to keep the contract aligned with deployed and documented behavior.
- (decision) Graceful CLI shutdown pauses active sessions before shutdown and startup resumes them in place; existing runtime behavior and user documentation are authoritative, and stale root SPEC.md passages that fail every active job on restart are superseded to preserve recoverable work.
- (decision) The canonical machine-readable restart reason token is server_restart so restart-originated state transitions and audit records use one stable value.
- (decision) TraceForge-native event behavior is authoritative: canonical SessionEvent.kind values are open dotted strings such as job.state_changed, permission.requested, and session.heartbeat; persistence and SSE deliver the TraceForge envelope and dotted kind without legacy snake_case translation, and transport-only keepalives remain distinct from canonical session.heartbeat domain events. Stale root SPEC.md event-vocabulary and heartbeat passages are superseded to preserve canonical event identity end to end.
- (event) Coherence validation PASS: Spec Law rules 1-6 and 8 remain satisfied; all 24 stable capability IDs retain WHAT intents and testable success criteria, the approved policy, restart, reason-token, and TraceForge semantics are internally consistent, and the resolved source-authority question is absent from live derived artifacts.
- (event) Preservation validation PASS: every approved decision and rationale lands in SPEC.md or a normative companion; stale root SPEC.md passages are explicitly recorded as superseded, historical memlog entries remain append-only, and no load-bearing prior deployment requirement or capability ID was dropped.
- (decision) Adopt architecture/ARCHITECTURE-SPINE.md as a normative companion owned by bmad-architecture; its final AD-1 through AD-32 resolve the Kubernetes architecture questions without changing or renumbering CAP-1 through CAP-24, and its tagged assumptions remain explicit correction points.
- (event) Coherence validation PASS: Spec Law rules 1-6 and 8 remain satisfied; all 24 stable capabilities retain WHAT intents and testable success criteria, the final architecture companion resolves the prior implementation questions, and no live kernel statement conflicts with AD-1 through AD-32.
- (event) Preservation validation PASS: CAP-1 through CAP-24 remain unique and unrenumbered; brownfield-constraints.md, mode-requirements.md, and architecture/ARCHITECTURE-SPINE.md all exist as normative companions; the adopted spine remains externally owned and preserves AD-1 through AD-32.
- (decision) Adopt the updated architecture/ARCHITECTURE-SPINE.md as the normative bmad-architecture-owned companion; downstream work must preserve every current stable decision AD-1 through AD-35 and its acceptance mapping without renumbering CAP-1 through CAP-24.
- (decision) A conforming Kubernetes baseline is Kubernetes-native: namespaced CodePlane CRDs and controller reconciliation are authoritative current control-plane state, and no PostgreSQL, S3, external database, or external object store is required.
- (decision) Kubernetes durable payloads and canonical history use standard CSI-backed PVC and StorageClass abstractions behind shared ports; CRDs retain bounded metadata/status/provenance only, and optional external storage adapters never become baseline requirements.
- (decision) The prior adoption entry protecting only AD-1 through AD-32 is superseded by the updated AD-1 through AD-35 architecture companion.
- (event) Coherence validation PASS: Spec Law rules 1-6 and 8 remain satisfied; CAP-1 through CAP-24 retain stable unique IDs, WHAT intents, and testable success criteria, and the Kubernetes-native no-external-state constraints align with the adopted architecture companion.
- (event) Preservation validation PASS: the updated architecture companion is adopted through AD-35, PostgreSQL/S3 rejection and CSI/CRD boundaries are preserved, and no capability was added, removed, reused, or renumbered.
- (event) Specification refreshed through memlog derivation in headless mode.
- (decision) Adopt the final architecture/ARCHITECTURE-SPINE.md as the normative bmad-architecture-owned companion; downstream work must preserve every current stable decision AD-1 through AD-37 and its acceptance mapping without changing or renumbering CAP-1 through CAP-24.
- (decision) AD-36 is authoritative for repository-derived intelligence: RepositoryIndexStoragePort owns immutable, identity-bound index generations as rebuildable non-authoritative caches, with dedicated build and query workloads plus reference-, lease-, integrity-, and policy-safe lifecycle behavior in both modes.
- (decision) AD-37 is authoritative for durable job/session and repository-context handoff: immutable handoff packages and conflict-preserving repository-context generations have explicit owners, provenance, compatibility, selection, publication, retention, recovery, backup/restore, and export/import guarantees before successor execution.
- (decision) The prior adoption entry protecting only AD-1 through AD-35 is superseded by the final AD-1 through AD-37 architecture companion.
- (event) Coherence validation PASS: Spec Law rules 1-6 and 8 remain satisfied; CAP-1 through CAP-24 are preserved exactly with stable unique IDs, WHAT intents, and testable success criteria, and the derived kernel consistently protects the final architecture companion through AD-37 including AD-36 and AD-37 ownership boundaries.
- (event) Preservation validation PASS: all current companions remain unchanged and discoverable; the normative companion statement and non-goal protect AD-1 through AD-37, the prior AD-1 through AD-35 adoption is explicitly superseded, and no capability was added, removed, reused, renumbered, or altered.
- (event) Specification refreshed through memlog derivation in headless UPDATE mode from the final AD-1 through AD-37 architecture companion.
- (decision) The canonical Kubernetes v1 baseline is one trusted tenant per installation namespace with one single-replica control-plane StatefulSet; SQLite on a private ReadWriteOncePod PVC is canonical for product intent, state, history, idempotency, and installation incarnation, while one installation-scoped RWX PVC holds repository, workspace, index, session-handoff, session, and artifact files.
- (decision) CodePlane CRD spec and status are bounded projection-only views of committed SQLite; authenticated APIs commit SQLite first, direct CRD edits are rejected or reverted, and etcd CR state is excluded from backup and rebuilt from SQLite after restore.
- (decision) AD-36 is simplified and authoritative: RepositoryIndexStoragePort owns rebuildable filesystem cache directories on RWX with identity/checksum manifests; one indexer Job publishes under a repository Lease, workers mount indexes and the shared mirror read-only, and no index CRD or query service exists in the baseline.
- (decision) AD-37 is simplified and authoritative: immutable handoff JSON/artifact files live under /sessions and /artifacts with SQLite metadata and control-plane publication; repository-context files live directly under the repository session-handoff path with Lease-serialized conflict-preserving publication; no handoff CRD or generation service exists in the baseline.
- (decision) HA, active/active control planes, multiple hostile tenants per installation, per-tenant storage gateways/shards, distributed index and handoff CRDs/services, and cross-tenant isolation are Deferred profiles that require new trust, storage, and coordination decisions; they are not baseline guarantees.
- (decision) The prior CRD-authoritative and immutable index/context-generation adoption entries are superseded by the finalized shared-volume architecture companion; CAP-1 through CAP-24 and AD-1 through AD-37 remain stable, complete, and unrenumbered.
- (event) Coherence validation PASS: Spec Law rules 1-6 and 8 remain satisfied; exactly 24 stable capabilities retain WHAT intents and testable success, and the kernel/mode companion consistently describe the single-tenant single-replica SQLite plus RWOP/RWX projection-only baseline and Deferred HA/multi-tenant profile.
- (event) Preservation validation PASS: CAP-1 through CAP-24 and AD-1 through AD-37 are present, stable, and unrenumbered; all three normative companions remain discoverable; simplified AD-36 filesystem indexes and AD-37 filesystem handoff/context ownership preserve the finalized architecture without stale CRD-authority or generation-service claims.
- (event) Specification refreshed through memlog derivation in headless UPDATE mode from the finalized shared-volume AD-1 through AD-37 architecture companion.
- (decision) Backup conformance requires the SQLite barrier to stop non-control writers, verify no non-control Pod retains any RWX read-write mount, and abort on timeout; restore excludes etcd projections, rotates installation incarnation, and rebuilds CRDs from SQLite.
- (event) Coherence validation PASS: the 24-capability kernel and mode companion consistently encode the finalized SQLite plus RWOP/RWX baseline, including the strengthened backup barrier and Deferred HA/multi-tenant profile.
- (event) Preservation validation PASS: CAP-1 through CAP-24 and AD-1 through AD-37 remain stable and all final shared-volume gate decisions are preserved in the kernel or normative architecture companion.
- (event) Specification refreshed through memlog derivation in headless UPDATE mode from the finalized shared-volume architecture.
- (decision) Adopt the finalized generic shared-file architecture companion through AD-37 without changing CAP-1 through CAP-24. CodeRecon indexes and session handoffs are representative fixtures only and do not define infrastructure primitives.
- (decision) The Kubernetes installation RWX contract is generic: SQLite owns every shared-file classification, metadata, path/hash/provenance reference, and publication state; RWX owns byte custody; SharedFileStoragePort is the operation boundary. Each file set declares scope, lifecycle class, writer/readers, behavior, hash/reference, atomic publication, retention/cleanup, backup/rebuild, and subPath access.
- (decision) Canonical RWX namespaces are /installation/shared, repository mirror/workspaces/shared roots, job artifacts/sessions roots, and /cache/<scope>/<identity>; there are no top-level /sessions or /indexes infrastructure namespaces.
- (decision) AD-36 now governs all intentionally published derived caches: identity manifest, checksums, atomic publish, read-only consumers, non-authoritative rebuild behavior, and Lease-serialized SQLite-state-first quarantine/recheck GC. CodeRecon, build outputs, analysis caches, and generated indexes are fixtures.
- (decision) AD-37 now governs durable artifacts and repository shared files: immutable or append-only publication, SQLite hash/provenance references, compatibility only when type-required, read-only consumption, and generation-plus-CAS conflict preservation for mutable singletons. Handoff selection/fencing remains an application rule only when consumed by a successor.
- (decision) Backup/export include authoritative shared data and durable artifacts, may omit/rebuild derived caches, and exclude disposable workspaces unless explicitly retained as durable artifacts. Future HA/multi-tenant storage metadata/services are generic choices deferred to that profile; dedicated CodeRecon or handoff machinery is not presumed.
- (event) Coherence validation PASS: exactly 24 stable capabilities retain intent and success; the five-field kernel and mode requirements consistently describe SQLite metadata/reference authority, RWX byte custody, the generic shared-file lifecycle, broad canonical namespaces, and representative-only CodeRecon/handoff fixtures.
- (event) Preservation validation PASS: CAP-1 through CAP-24 and AD-1 through AD-37 remain stable and unrenumbered; the approved single-tenant single-replica RWOP/RWX projection-only baseline, local independence, supervised default, server_restart, TraceForge dotted events, backup barrier, and no-external-state constraints are preserved.
- (event) Specification refreshed through memlog derivation in headless UPDATE mode from the finalized generic shared-file and artifact lifecycle architecture.
- (direction) Headless UPDATE hardens the finalized architecture into an actionable implementation contract; do not redesign architecture, implement code, create epics/stories, alter dependencies, commit, or push.
- (decision) conformance-requirements.md is the normative REQ-1-based machine contract; each requirement binds CAP and AD IDs, an allowed verification method, and required evidence.
- (decision) failure-modes.md is normative for material failure triggers, externally observable conditions, automatic action, prohibited false success, recovery ownership, fixtures, and REQ traceability.
- (decision) delivery-slices.md is normative for seven additive, demonstrable implementation slices that preserve local conformance and avoid a big-bang rewrite.
- (decision) Operator/provider selections remain bounded decisions DEC-1 through DEC-7 with named owners and slice deadlines; they do not reopen AD-1 through AD-37 or invent providers.
- (event) Coherence validation PASS: exactly 24 stable capabilities retain WHAT-only intents and now have observable verifiable success criteria; 58 stable normative requirements cover shared/local/Kubernetes semantics, data lifecycle, failures, delivery, evidence, and bounded operator choices without conflicting ownership.
- (event) Preservation validation PASS: CAP-1 through CAP-24 and AD-1 through AD-37 remain stable and mapped; the finalized architecture is normative, every material failure maps to REQs, and local supervised/server_restart/TraceForge plus single-tenant RWOP/RWX projection-only constraints are preserved.
- (decision) REQ-59 closes four-tier configuration precedence as defaults < installation < repository < job in both modes, with observable source-tier evidence and supervised at defaults.
- (decision) AD-25 resolves v1 parity: native mirroring, workstation PTY, local transcription, tunnels, and localhost preview remain local-only; Kubernetes uses the fixed managed analogues and capability discovery reports every difference.
- (event) Context-separated implementation-readiness review found no blocking issues and two high ambiguities; both highs and the clear traceability/failure-behavior findings were corrected in the derived contract and recorded in reviews/review-actionable-spec-final.md.
- (event) Coherence validation PASS: 24 capabilities, 59 requirements, 25 material failure modes, seven delivery slices, and seven bounded decisions are internally consistent, verifiable, and ownership-complete; no blocking or high review finding remains.
- (event) Preservation validation PASS and specification finalized: CAP-1 through CAP-24 and AD-1 through AD-37 are stable and individually traced; all finalized local, Kubernetes, storage, security, recovery, portability, and Deferred-profile decisions are preserved without architecture redesign.
