---
scope: Dual-mode CodePlane architecture for Kubernetes-native and local-daemon operation
purpose: build-substrate
altitude: feature
updated: 2026-08-05T16:03
---

- (decision) AD-1 binds all capabilities: use a ports-and-adapters modular control plane with a durable event log and an isolated per-job execution plane; shared domain semantics depend only on ports, while local-daemon and Kubernetes supply adapters; prevents mode forks.
- (decision) AD-2 binds CAP-1 CAP-2 CAP-22 CAP-24: local-daemon remains an independent single-process FastAPI service using SQLite, local filesystem, local Git worktrees, local credentials, and in-process events; Kubernetes or hosted CodePlane is never required; prevents local mode becoming a client of cluster mode.
- (decision) AD-3 binds CAP-4 CAP-15 CAP-18: Kubernetes control-plane components never execute agent tools; one isolated workload Pod per active job owns the workspace and agent process, reached through a versioned worker protocol; prevents control-plane compromise from directly becoming repository execution.
- (assumption) AD-4 [ASSUMPTION] binds CAP-20: a Kubernetes installation is one instance; tenant means team or organization and maps to one dedicated execution namespace, while control services occupy an installation namespace; all durable rows and object keys carry tenant_id; prevents ambiguous namespace and data ownership.
- (decision) AD-5 binds CAP-7 CAP-20: Kubernetes authenticates humans with operator-configured OIDC and maps groups to instance_admin tenant_admin operator reviewer viewer; every mutation and read authorizes tenant repository job and action scope; agents and service accounts cannot approve their own actions; delegation is explicit scoped expiring revocable and audited.
- (decision) AD-6 binds CAP-7 CAP-15 CAP-20: each control component and tenant execution plane has a distinct Kubernetes service account with projected short-lived tokens; worker Pods disable automount unless Kubernetes API access is explicitly required and receive no cluster-wide credential; prevents shared ambient service identity.
- (decision) AD-7 binds CAP-4 CAP-15 CAP-18: Kubernetes v1 supports in-cluster managed execution only; workloads run non-root with read-only root filesystem, dropped capabilities, RuntimeDefault seccomp, no privilege escalation or host mounts, per-job writable volume, default-deny network policy, declared requests limits deadlines and ephemeral-storage bounds; locality is persisted.
- (decision) AD-8 binds CAP-3 CAP-17 CAP-23: RepositoryPort and WorkspacePort preserve GitService semantics; Kubernetes clones/fetches a registered remote into a tenant-scoped read-only bare-object cache then materializes a private per-job workspace volume; already-local paths are not mountable into Kubernetes; resolution exports commit SHA and patch or bundle before merge PR or discard.
- (decision) AD-9 binds CAP-3 CAP-6 CAP-15 CAP-17: repository and agent credentials are resolved just in time from an operator-owned secret reference, mounted as read-only files on memory-backed volumes, never persisted in job records or caches, and revoked after job completion; URL credentials are forbidden; prevents credential copying into hosted or durable product state.
- (decision) AD-10 binds CAP-5 CAP-9 CAP-19: PostgreSQL is Kubernetes durable system of record for jobs approvals policy canonical events projections leases and audit; S3-compatible object storage owns artifacts exports and retained workspace outcomes; job workspaces and caches are reconstructable and never authoritative.
- (decision) AD-11 binds CAP-2 CAP-5 CAP-9: every authoritative state mutation and its canonical TraceForge SessionEvent append commit in one database transaction; event kinds stay open dotted strings and envelopes pass verbatim; events have immutable UUID, tenant_id, job_id, per-job sequence, and storage-local global cursor; prevents dual writes and translation drift.
- (decision) AD-12 binds CAP-9: delivery is at least once with strict committed order per job; consumers deduplicate by event UUID, resume by storage-local cursor, and fall back to an atomic snapshot plus subsequent cursor when outside retention; slow SSE clients are disconnected to replay rather than silently dropped; transport keepalives are never persisted domain events.
- (decision) AD-13 binds CAP-8 CAP-12 CAP-18: scheduling uses PostgreSQL queue rows and leased claims with fencing tokens and FOR UPDATE SKIP LOCKED; weighted fair selection is tenant then repository then FIFO, exactly one valid execution lease exists per job, heartbeats renew it, and expiry drives explicit recovery; prevents duplicate workers and starvation.
- (decision) AD-14 binds CAP-8 CAP-12 CAP-18: cancellation is durable intent before worker termination; workers checkpoint outcome and stop within grace, then force deletion records cause; finalizers capture artifacts revoke secrets release leases delete workspace and cache references idempotently; cleanup never erases evidence needed by retention policy.
- (assumption) AD-15 [ASSUMPTION] binds CAP-8 CAP-18 CAP-21: initial supported envelope is 200 concurrent jobs per installation, 50 per tenant, 5000 queued jobs, and 2000 concurrent SSE clients; quotas exist per tenant repository and identity and reject or queue deterministically; prevents an untestable scale claim.
- (assumption) AD-16 [ASSUMPTION] binds CAP-5 CAP-11 CAP-19: default retention is canonical events and audit 365 days, transcripts and telemetry 90 days, artifacts and workspace outcomes 30 days, caches 7 idle days; legal hold overrides deletion; prevents backend-specific retention drift.
- (assumption) AD-17 [ASSUMPTION] binds CAP-12 CAP-19 CAP-21: Kubernetes service objective is 99.9 percent monthly control-plane availability, p95 query under 500 ms, accepted command under 1 s, committed event visible by SSE under 2 s, RTO 60 minutes, RPO 5 minutes; PostgreSQL PITR and daily verified backups plus object versioning meet recovery; local mode has crash-consistent restart rather than HA SLO.
- (decision) AD-18 binds CAP-11 CAP-21: all components emit structured logs, Prometheus metrics, and OTLP traces with tenant_id job_id session_id event_id request_id and execution_id correlation; audit is immutable application data, not inferred from logs; health separates live ready and dependency-degraded states.
- (decision) AD-19 binds CAP-10 CAP-15 CAP-21: Kubernetes exposes one ClusterIP control service behind operator-managed networking.k8s.io/v1 Ingress or Gateway; TLS is mandatory off-cluster, DNS and certificates are operator-owned, SSE disables buffering and honors reconnect cursors, MCP uses authenticated Streamable HTTP, terminals use authenticated WebSocket to the job worker, and previews use an authorized same-origin reverse proxy.
- (decision) AD-20 binds CAP-6 CAP-15 CAP-20: built-in Kubernetes Secrets are the required baseline and Secrets Store CSI references are optional; secret values never enter configuration API events logs UI or artifacts; rotation updates references and new mounts, while active jobs either keep the leased version or are explicitly restarted; prevents partial credential rotation.
- (decision) AD-21 binds CAP-16 CAP-21: ship an OCI-distributed Helm chart using chart API v2, namespace-scoped RBAC, NetworkPolicies, PodDisruptionBudgets, migrations as a pre-upgrade Job, and explicit external PostgreSQL and S3 endpoints; production packaging does not install an opaque database or object store.
- (version) Kubernetes supported baseline at authoring is maintained minors 1.34 through 1.36, verified from kubernetes.io releases on 2026-08-05; Linux amd64 is required and arm64 is [ASSUMPTION] supported when all selected agent images publish arm64.
- (version) PostgreSQL Kubernetes baseline is major 17 at its current supported minor, verified from postgresql.org versioning on 2026-08-05; existing local mode retains SQLAlchemy 2.x plus SQLite WAL.
- (version) Helm package format is chart API v2 in an OCI registry; official Helm chart documentation verified the versioned chart format on 2026-08-05.
- (decision) AD-22 binds CAP-13 CAP-16 CAP-19: releases use expand-migrate-contract database changes; control and worker protocols support N and N-1 during rolling upgrade; migration dry-run and backup freshness gate upgrades; rollback is supported until contract migration, after which restore or forward-fix is required; unsupported downgrade fails before mutation.
- (decision) AD-23 binds CAP-14: export is a versioned signed manifest plus JSONL TraceForge events, JSON configuration and policy without secrets, artifact blobs, repository bindings, and optional Git bundle; canonical UUIDs and per-job sequence survive import while storage cursors and secret references are remapped; collisions are idempotent only when hashes match, otherwise rejected.
- (decision) AD-24 binds CAP-3 CAP-14: concurrent mutation of the same repository and target ref is prohibited within an instance by a repository-ref lease; two independent local or Kubernetes instances cannot safely coordinate and operators must assign ownership; prevents false cross-instance consistency.
- (decision) AD-25 binds CAP-23 CAP-24: native CLI mirroring, workstation PTYs, local voice transcription, Dev Tunnel, Cloudflare tunnel, and localhost preview remain local-only in the first Kubernetes release; Kubernetes provides managed job terminal and preview analogues, authenticated sharing, and optional Web Push with durable VAPID secret, but does not ingest developer-machine sessions.
- (decision) AD-26 binds CAP-1 CAP-2 CAP-13 CAP-14: migration phases are ports and conformance extraction, PostgreSQL and object adapters, Kubernetes worker protocol and scheduler, identity ingress packaging, then export import and hardening; each phase keeps local adapters green and API event schema backward compatible.
- (decision) AD-27 binds all capabilities: FastAPI thin routes, service orchestration, repository-only persistence, AgentAdapterInterface, GitService, Pydantic CamelModel API contracts, domain events, Zustand selectors, central SSE dispatcher, generated TypeScript types, and virtualized large lists remain mandatory shared conventions.
- (decision) AD-28 binds CAP-1 through CAP-24: release acceptance is a decision-to-capability matrix plus shared conformance, mode lifecycle, isolation, authorization, event replay, recovery, upgrade rollback, backup restore, scale, ingress, secret rotation, export import, and intentional-difference tests; prevents architecture claims without executable evidence.
- (event) architecture decisions distilled; reviewer gate pending
- (decision) AD-5 is strengthened: approval delegation validity, scope, expiry, revocation state, and version are read under lock and checked inside the same transaction that commits the approval and audit event; cached authorization cannot authorize a commit.
- (decision) AD-7 and AD-9 are strengthened: every worker egress destination and mounted credential is derived from and bound to the job declared repository/provider resources; undeclared destinations, cluster metadata, link-local, private control networks, and credentials broader than the declared resource are denied; worker images are immutable-digest pinned.
- (decision) AD-8 is strengthened: only a trusted repository acquisition service may write a cache; jobs mount cache content read-only; remote identity and Git object integrity are validated before atomic cache promotion; a recovery Git bundle is durably stored before remote mutation and retained until resolution is durably confirmed.
- (decision) AD-10 is strengthened: cross-store artifact lifecycle uses persistence-owned staged, available, degraded, tombstoned states; metadata never reports availability before checksum-confirmed object commit and failed or missing object operations remain explicit durable evidence.
- (decision) AD-11 is strengthened: current-state validation, row locking, transition, idempotency claim, canonical event append, per-job sequence, and audit commit in one unit of work; canonical event integrity is further bound by new AD-31.
- (decision) AD-12 is strengthened: external cursors are opaque authenticated UTF-8 tokens bound to tenant, instance, stream scope, and adapter position; mismatch fails without cross-tenant existence disclosure. Snapshot state and exact high-water boundary are read in one transaction and replay begins strictly after that boundary. The reviewer proposed inclusive replay was rejected because it creates duplicates rather than fixing boundary atomicity.
- (decision) AD-13 is strengthened: every callback validates attempt and fencing token against the current unexpired locked lease before mutation; expiry detection, interruption transition, and event emission have one transactional winner across scheduler replicas.
- (decision) AD-19 is strengthened: preview and server-side fetch paths deny loopback, link-local, cloud metadata, cluster service, RFC1918/ULA, control-plane, and operator-configured private ranges after DNS resolution and on every redirect, with DNS rebinding resistance.
- (decision) AD-22 is strengthened: worker protocol semantic versions are negotiated before lease issuance; N supports N-1; rename, removal, requiredness, or type changes are version changes; contract migrations are explicitly labeled removal/meaning changes that previous releases read.
- (decision) AD-23 is strengthened: cross-instance exports are signed and imports verify an operator-trusted source key before mutation; unsigned packages are accepted only as same-instance local disaster-recovery backups under explicit reduced-trust operator action with an immutable audit record and never as trusted cross-instance history; source cursors are non-portable and force snapshot on the target.
- (decision) AD-27 is strengthened: supervised remains the default policy in both modes and changing the shipped default requires an intentional-difference contract.
- (decision) AD-29 binds CAP-4 CAP-7 CAP-12 CAP-15 CAP-18 CAP-20: worker and control plane mutually authenticate; a short-lived audience-restricted worker identity is bound to tenant, job, Pod UID, attempt ID, current lease/fence, scheduling record, and protocol version, verified against authoritative records before any callback; workers trust only the configured control identity; prevents namespace-token impersonation and tenant-claim forgery.
- (decision) AD-30 binds CAP-5 CAP-7 CAP-15 CAP-20: Kubernetes persistence enforces tenant isolation at the database boundary with PostgreSQL RLS and transaction-local tenant binding; repository contracts require tenant context and fail closed when missing or mismatched; privileged maintenance paths are separate and audited; local mode uses one explicit implicit tenant; prevents omitted filters from leaking data.
- (decision) AD-31 binds CAP-2 CAP-5 CAP-9 CAP-11 CAP-14 CAP-19: each canonical event stores a per-job cryptographic hash over a versioned canonical encoding of event identity, tenant, job, sequence, prior hash, kind, payload, metadata, and provenance; genesis and algorithm identifiers are fixed by export schema; append validation is transactional and replay/export/restore verify the chain; prevents undetected insertion deletion reordering or mutation.
- (decision) AD-32 binds CAP-3 CAP-6 CAP-15 CAP-23 CAP-24: local-daemon trust ends at the logged-in OS user; managed agents inherit that user authority and current daemon cannot claim sandboxing. CodePlane provides best-effort per-job working directories, minimum declared credential injection, path allowlisting, redaction, and process cleanup, but cannot prevent agent access to user-readable files or credentials; stronger isolation requires a separately qualified sandbox adapter.
- (decision) Local SQLite backup is architecture-bound: a valid backup is produced either after clean daemon shutdown and WAL checkpoint or through the SQLite online backup API from one consistent snapshot including required WAL state; copying a live database or ~/.codeplane tree is unsupported.
- (decision) Remaining reviewer resolutions upheld: AD-3 and AD-22 define negotiated canonical worker message schemas; local and Kubernetes command idempotency is persistence-atomic; local interruption resumes through server_restart rather than lease requeue; arm64 is not claimed until every selected agent image digest passes a release qualification matrix; CAP-11 traceability includes scale and SLO decisions; AD-12 names backpressure/HOL prevention; AD-11 names ephemeral delta non-authority.
- (decision) Reviewer implementation mechanics deferred with revisit gates: exact RLS policy SQL, artifact staging tables, cursor cryptographic encoding, hash-chain canonicalization library, cache eviction and tombstone compaction, and admission signature controller are adapter design choices only after conformance tests encode the governing AD behavior; no security guarantee is deferred.
- (event) All prior reviewer findings triaged; seven security and data-integrity findings closed in architecture decisions; re-distillation pending.
- (decision) AD-7 is further strengthened after current-fit review: native NetworkPolicy alone cannot enforce FQDN/provider egress or all metadata denial. Kubernetes requires a conforming NetworkPolicy implementation, routes worker external traffic only through an authenticated job-policy egress gateway, and requires platform node-metadata protection; no provider-specific CNI CRD is required.
- (decision) AD-30 is strengthened: tenant tables use FORCE ROW LEVEL SECURITY; API runtime roles are non-owner non-superuser and lack BYPASSRLS; migration ownership is separate. Cross-tenant fair scheduling uses a minimal installation-scoped queue projection and narrowly scoped claim function rather than tenant-table RLS bypass.
- (decision) AD-31 is strengthened: retention truncation commits a signed or audit-chain-protected checkpoint root with sequence hash and time before deleting prefix events; each event stores its canonicalization version; incremental import must match the target join hash before append.
- (decision) AD-18 audit records join a separate installation audit hash chain or canonical event chain and are verified on export and restore; AD-16 bounds tombstone creation/compaction while retaining checkpoint and audit deletion evidence.
- (decision) AD-10 fixes artifact content identity as SHA-256 of plaintext bytes before transport or storage transforms, never an object-store ETag; local backup captures SQLite and artifact tree under one retention pause and verifies missing blobs as degraded before scheduling resumes.
- (decision) AD-8 cache promotion is generation-based copy-on-write: verified content lands at a new immutable path; future acquisitions switch atomically; active generations are never repacked or garbage-collected until reference count reaches zero.
- (decision) AD-13 fencing token is fixed for one claimed attempt; renewal extends expiry only; a new token is minted only for a new claim. Scheduler cross-tenant ranking uses the AD-30 minimal installation queue projection.
- (decision) AD-29 configured control identity means a stable Service-level trust anchor, not a leaf certificate or replica; every connection revalidates the current chain so rotation and replica replacement do not break attempts.
- (decision) AD-22 is strengthened: every replica supports each active attempt negotiated version for its lifetime, and pre-upgrade checks block overlapping rollout below the active protocol floor until attempts drain or are explicitly interrupted.
- (decision) AD-11 is strengthened against lost updates: every authoritative row mutation uses lock or version compare and column-scoped updates inside the unit of work; legal hold and retention fields cannot be overwritten by lifecycle transitions.
- (decision) AD-12 distinguishes replay from retention and returns an explicit replay_window_exceeded snapshot condition; AD-19 binds terminal liveness to worker and scheduling record rather than API replica.
- (decision) AD-28 acceptance mapping is strengthened: every AD must govern at least one acceptance criterion and CI validates coverage; AD-3 and AD-26 receive direct protocol-boundary and migration-phase acceptance coverage.
- (decision) Delegated approval authority cannot be re-delegated; this closes unbounded delegation-chain ambiguity without changing the role model.
- (event) Fresh reviewer gate findings triaged; all clear architecture-level fixes selected for re-distillation, with provider-specific CNI, SQL schema, proxy implementation, cache layout, and compaction mechanics deferred behind conformance tests.
- (event) spine finalized
- (decision) User rejects PostgreSQL and S3 lock-in; a conforming Kubernetes baseline must require neither external database nor external object store.
- (decision) AD-4 is amended: tenant isolation in Kubernetes is namespaced resource ownership plus Kubernetes RBAC and mandatory tenant identity checks in every API and controller reconciliation; local mode retains one explicit implicit tenant.
- (decision) AD-10 is amended: namespaced CodePlane CRDs and their status subresources own bounded authoritative Kubernetes current state, lifecycle, references, and provenance; large durable payloads, transcripts, artifact bytes, exports, and retained workspace outcomes are owned through a storage port whose baseline Kubernetes adapter uses CSI-backed PVCs and whose local adapter uses filesystem storage.
- (decision) AD-11 is amended: Kubernetes cannot claim multi-object database transactions. Each mutation is an idempotent single-resource API write guarded by resourceVersion or generation preconditions; controllers converge dependent resources through explicit conditions and observedGeneration, and durable history records intent and completion around cross-resource sagas.
- (decision) AD-12 is amended: CRD status is the observable current-state snapshot while a bounded append-only per-job history on the storage port is canonical durable replay and audit provenance; entries have per-job monotonic sequence, immutable ID, TraceForge dotted kind, hash-chain fields, and a CRD-published high-water mark, with retention checkpoints and explicit replay_window_exceeded recovery.
- (decision) AD-13 is amended: controllers watch CodePlane CRDs and create exactly one Kubernetes Job or Pod per execution attempt; leader election uses coordination.k8s.io Lease where needed, attempt fencing binds worker identity to attempt UID and token in authoritative attempt status, and stale callbacks are rejected by current resourceVersion/status comparison before mutation.
- (decision) AD-14 is amended: deletion and cancellation use desired-state intent, ownerReferences, finalizers, propagation policy, explicit cleanup conditions, and idempotent reconciliation; finalizers never retain unbounded payloads or erase retention evidence.
- (decision) AD-17 is amended: Kubernetes backup and recovery use Kubernetes API resource exports plus storage-port snapshots; CSI VolumeSnapshot is used when supported, with quiesced filesystem-level fallback, checksum and chain verification, and no baseline PITR or object-versioning dependency.
- (decision) AD-21 is amended: the baseline package is OCI Helm with CRDs, namespace-scoped controllers, RBAC, Services, NetworkPolicies, PodDisruptionBudgets, StorageClass/PVC configuration, and optional VolumeSnapshot integration; no external state-service prerequisite is allowed.
- (decision) AD-22 and AD-26 are amended: upgrades use versioned CRD schemas, conversion/defaulting compatibility, storage-format migration reconciliation, protocol N/N-1, and additive phases for ports, CRDs/controllers, workers, ingress/operations, and interoperability; PostgreSQL schema and S3 adapter phases are removed.
- (decision) AD-23 and AD-31 are amended: export/import and integrity operate over CRD manifests plus storage-port history/artifacts, preserving canonical IDs, sequence, hashes, and provenance while remapping Kubernetes UIDs/resourceVersions and storage references.
- (decision) AD-29 and AD-30 are amended: worker trust is bound to Pod UID, execution-attempt UID, namespace, service account, attempt fence, and protocol version; namespace/RBAC isolation and controller/API tenant checks replace PostgreSQL RLS entirely.
- (decision) AD-33 binds CAP-5 CAP-7 CAP-8 CAP-9 CAP-12 CAP-13 CAP-16 CAP-18 CAP-19 CAP-20 CAP-21: Kubernetes API resources and reconciliation are the authoritative cluster control-plane model. Namespaced CRDs cover CodePlaneJob, CodePlaneApproval or policy references, CodePlaneRepositoryBinding, CodePlaneExecutionAttempt, and bounded status/provenance; all use spec/status separation, observedGeneration, conditions, finalizers, ownerReferences, resourceVersion preconditions, and server-side apply field ownership.
- (decision) Optional external object-store adapters may implement the storage port but are never required or named as the Kubernetes baseline; the operator-selected CSI implementation remains outside CodePlane.
- (decision) Accepted invariants remain: authenticated egress gateway, isolated workers, TraceForge dotted event semantics, supervised default, local independence, existing application conventions, and stable CAP-1 through CAP-24 mapping.
- (decision) Reviewer gate requires a concrete baseline storage topology: each tenant receives a chart/controller-managed single-active storage gateway over tenant-scoped RWO PVCs; APIs and workers use the authenticated storage port and never directly mount canonical history/artifact PVCs; attempt workspaces remain private RWO/RWOP PVCs.
- (decision) AD-31 is strengthened: the storage gateway exposes atomic compare-and-append over expected sequence/hash and stable operation/event identity, fsyncs canonical bytes and head metadata before acknowledgment, fences failover with a preconditioned storage-shard writer epoch, and rejects stale callback operations before canonical append.
- (decision) AD-33 is strengthened with a closed v1 namespaced CRD inventory including TenantBinding, Job, Approval, RepositoryBinding, ExecutionAttempt, RepositoryRefLock, Operation, ImportSession, BackupEpoch, and StorageShard, plus fixed field/status/condition/finalizer ownership and bounded lifecycle budgets.
- (decision) AD-13 is strengthened: a resourceVersion CAS on CodePlaneJob.status reserves one deterministic claim generation before any child is created; attempt and Job names derive from job UID plus claim generation; Jobs use parallelism 1, completions 1, backoffLimit 0, restartPolicy Never; Pod replacement is not the same attempt.
- (decision) AD-29 is strengthened with two-stage bootstrap: worker presents projected audience token plus Downward API namespace/name/Pod UID; API TokenReviews and live-verifies Pod to Job to Attempt ownership and active claim, then issues short-lived attempt credential bound to attempt fence; fence is not itself a bearer secret.
- (decision) AD-14 is strengthened: one job cleanup orchestrator owns an ordered durable cleanup phase machine; finalizers are assigned to one external cleanup responsibility, are adoptable across N/N-1, use bounded retries, and permit audited break-glass only after a retention-safe tombstone is committed.
- (decision) AD-17 is strengthened: baseline RTO/RPO covers Pod/node/logical deletion while the CSI backend survives; cluster/storage-backend disaster recovery is a separately qualified operator profile with independent copy. BackupEpoch fences writers, records per-job heads, snapshots each PVC, rereads CRD versions, and rejects mismatched epochs.
- (decision) AD-21 and AD-22 are strengthened: chart installs the authenticated egress gateway; operator supplies cluster identity/ingress/network enforcement prerequisites. CRDs ship as a separate versioned OCI bundle applied by an explicit preflighted SSA upgrade workflow before the application chart; ordinary Helm CRD install/rollback is not claimed to manage schema lifecycle.
- (decision) AD-15 is strengthened with etcd/API budgets: serialized CRDs at most 256 KiB, fixed managers and condition types, no queue-position churn, terminal child TTL/archive rules, and load qualification measures object counts, list/watch payload, API latency, write rate, and etcd growth.
- (decision) AD-11 is strengthened with a durable bounded CodePlaneOperation resource/ledger linking idempotency key, request digest, participants, event identity, phases, and original result; replay after the supported window fails explicitly rather than re-executing.
- (decision) AD-23 is strengthened with CodePlaneImportSession quarantine: imported resources remain scheduling-disabled until bytes, history, references, policy, and collisions verify and each resource is activated by a preconditioned final step.
- (decision) Controller topology is fixed: an installation bootstrap/admission controller has narrowly enumerated cluster permissions for namespace enrollment and summary/admission fields; tenant reconcilers and storage gateways run in tenant namespaces with Roles; human authorization remains application OIDC policy while Kubernetes RBAC governs workload identities.
- (decision) Current-fit fixes: Gateway API is gateway.networking.k8s.io/v1; FastAPI seed follows the locked 0.136.3 baseline; Helm CLI qualification covers 3.19.x and 4.2.x with chart API v2.
- (decision) Acceptance is strengthened for CAP-22 local CLI lifecycle and for CRD ownership, attempt races, history append, backup epochs, install profiles, etcd budgets, CRD upgrade/rollback, and finalizer escape behavior.
- (decision) AD-34 binds CAP-5 CAP-9 CAP-11 CAP-15 CAP-19 CAP-21: baseline Kubernetes durable-byte topology is one single-active tenant storage gateway over tenant-scoped RWO PVCs, with fenced failover and authenticated port access; API/controller/worker replicas never share-mount canonical history/artifact PVCs, while each worker alone mounts its private attempt workspace PVC.
- (decision) AD-35 binds CAP-7 CAP-8 CAP-13 CAP-16 CAP-20 CAP-21: controller topology separates narrowly privileged installation bootstrap/admission from tenant-local reconcilers; human OIDC authorization remains application-level, Kubernetes RBAC governs service identities, and the CRD OCI lifecycle is explicit rather than delegated to ordinary Helm rollback.
- (decision) Final gate fix: AD-34 baseline storage gateway requires ReadWriteOncePod-capable qualified CSI storage; takeover remains unavailable until old Pod termination and volume detachment/exclusive remount are proven, then atomically advances a durable PVC head epoch. No new gateway serves while stale write access is possible.
- (decision) Final gate fix: active-claim cancellation/replacement first advances a gateway-owned per-job callback epoch under the same sequencer as history append, then projects the claim to CRD status; callback append conditions on that durable epoch, closing the CRD-read-to-PVC-write race.
- (decision) Final gate fix: AD-33 closed inventory adds CodePlanePolicyBinding and removes all cross-namespace references/grants. A normative per-kind ownership/lifecycle table fixes spec/status managers, conditions, parent, bounds, and GC.
- (decision) Final gate fix: execution attempt owns one direct Pod rather than a Kubernetes Job; the accepted Pod UID is CAS-bound in attempt status before credential issuance and every other Pod is rejected.
- (decision) Final gate fix: CRD v1 evolution remains additive without conversion webhooks. Future conversion deploys and proves the N/N-1 webhook first, then mutates CRDs under a resourceVersion-CAS lifecycle claim on the cluster-scoped CRD; uninstall is blocked until retained objects no longer require a retiring webhook.
- (decision) Final gate fix: per-kind terminal ages/counts and installation API/etcd thresholds are numeric; admission rejects before exceeding them and sustained churn must converge after archive/GC.
- (decision) Final gate fix: local SQLite is canonical current state and TraceForge history; local filesystem owns large artifacts only. Kubernetes CRD status plus storage-gateway HistoryPort/PVC remains the cluster composition.
- (decision) Final gate fix: CodePlaneJob bounded projection is the sole convergent current-state snapshot; multi-resource detail responses carry a participant UID/resourceVersion vector and never claim one Kubernetes point-in-time snapshot.
- (decision) Final gate fix: replay cursors bind history/checkpoint epoch plus last sequence/hash; compaction pins or explicitly invalidates active replay epochs, and checkpoint verification material outlives all dependent suffixes, holds, exports, and backups.
- (decision) Final gate fix: BackupEpoch acknowledgement drains accepted operations to committed projection or parks them only in enumerated recovery-safe phases before capture.
- (decision) Final gate fix: chart-supplied egress gateway is HA/readiness-gated; outage pauses admission, preserves active attempt/quota without consuming retry budget, and differs from policy denial in conditions/audit.
- (decision) Final gate fix: canonical SPEC adoption must protect all current stable architecture decisions through AD-35 while preserving CAP-1 through CAP-24.
- (decision) Integrity verification fix: per-job callback epoch is atomically persisted and fsynced with gateway head metadata before claim replacement/cancellation is projected, and takeover verifies it.
- (decision) Integrity verification fix: job projection/history prefix names the expected participant UID/resourceVersion/operation vector; detail reads must assemble exactly that vector or retry/degrade.
- (decision) Integrity verification fix: BackupEpoch and ImportSession CRDs retain only bounded summary and checksummed storage references; immutable participant/phase manifests live through the storage port with head/version replay semantics.
- (decision) Integrity verification fix: local backup uses a mutation barrier that drains or safely parks operations and freezes artifact writers; SQLite snapshot plus artifact copy is a validated manifest set, not falsely atomic, and drift rejects capture.
- (event) Final reviewer gate PASS: architecture lint has zero findings; independent Kubernetes-native, data-integrity, and operability reviews report no remaining critical or high blockers.
- (event) Updated spine finalized with stable AD-1 through AD-35.
- (decision) AD-36 binds CAP-3 CAP-5 CAP-11 CAP-12 CAP-14 CAP-17 CAP-19 CAP-21 CAP-23 CAP-24: repository-derived intelligence is a rebuildable non-authoritative cache owned through RepositoryIndexStoragePort. Kubernetes uses bounded CodePlaneRepositoryIndex metadata/status, tenant PVC bytes, a dedicated indexer workload, and an authenticated tenant CodeRecon/index query service or injected MCP/tools; attempt Pods never own or mutate shared index bytes.
- (decision) AD-36 identity binds tenant, canonical repository identity, immutable source commit/tree, worktree or overlay identity, CodeRecon/index schema version, feature configuration, and model/tool digest. Base generations are immutable and tenant-shareable; attempt/worktree overlays are private, reference one base, and are atomically published only after integrity verification.
- (decision) AD-36 lifecycle maps current CodeRecon behavior explicitly: ensure_repo_indexed selects or builds an exact base generation; register_worktree creates a private overlay; reindex and sync_from_git build a successor overlay generation from declared deltas and pinned Git identity; merge_index creates a new verified target generation and only then retires the source overlay; drop_worktree releases the overlay lease/reference and schedules deletion only after no references remain.
- (decision) AD-36 conditions and recovery are explicit: Absent, Building, Ready, Stale, IntegrityFailed, RebuildRequired, and GarbageCollecting are generation-matched bounded conditions. Missing, stale, schema/config/model mismatch, or failed integrity never falls back to an incompatible index; the control plane deterministically selects an exact compatible generation or requests rebuild, while capability policy decides whether execution waits or proceeds without derived intelligence.
- (decision) AD-36 retention and recovery: index generations are content-addressed or identity-keyed, integrity-checked on publish/read/restore, reference-counted and leased by attempts/overlays, and GC only after all durable references and live leases expire. Index bytes are excluded from authoritative backup/export baseline and are rebuilt after restore/import; optional cache transfer is accepted only after complete identity and integrity revalidation.
- (decision) AD-37 binds CAP-2 CAP-5 CAP-9 CAP-12 CAP-14 CAP-19 CAP-23 CAP-24: immutable versioned job/session handoff packages contain summary, changed-file set, plan and curated-context references, source job/session/attempt, repository/ref/tree identity, content hash, provenance, intended consumer, and compatibility version. Bounded CodePlaneSessionHandoff metadata/status references bytes owned by ArtifactStoragePort; context.handoff references the immutable package.
- (decision) AD-37 replacement and follow-up attempts deterministically select an explicitly requested package or the newest compatible committed package for the same lineage and intended consumer, validate hash, provenance, repository/tree compatibility and format compatibility, then materialize or inject it before execution. Incompatible or missing packages produce explicit conditions and reject or require an explicit no-handoff policy; Pods may disappear without data loss and packages are never silently overwritten.
- (decision) AD-37 separately owns repository-scoped session-handoff protocol files through RepositoryContextPort. The local adapter is the actual Git common-directory session-handoff tree. Kubernetes stores immutable versioned repository-context generations through the tenant storage gateway, materializes the selected generation into each attempt private Git common directory before startup, and publishes checkpoint or termination changes using parent-generation and content-hash preconditions.
- (decision) AD-37 concurrent repository-context publication never uses last-writer-wins: disjoint changes may merge only through deterministic three-way validation; conflicting decisions, environment, or handoff records preserve both candidate generations, mark Conflict, and require explicit resolution that publishes a descendant. Thus decisions, environment, and handoff records remain available across Pods, workspaces, worktrees, and sessions.
- (decision) AD-37 backup, retention, and portability: handoff packages and repository-context generations are authoritative retained artifacts covered by backup/restore and legal hold, included in signed export/import with hashes, provenance, compatibility, lineage and identity remapping, and restored before resumable scheduling. Retention cannot delete a selected package or repository-context ancestor referenced by a live/held job, attempt, export, or descendant.
- (decision) AD-33 closed v1 inventory is amended to add CodePlaneRepositoryIndex and CodePlaneSessionHandoff with bounded metadata/status only; AD-10 storage ownership is amended to name RepositoryIndexStoragePort, ArtifactStoragePort, and RepositoryContextPort without adding PostgreSQL, S3, or another external baseline state service.
- (decision) AD-26 migration and AD-28 acceptance are amended to add index-storage/query/indexer and handoff/repository-context ports and adapters, then gate reuse, invalidation, rebuild, integrity, lease-safe GC, Pod-loss survival, deterministic selection, compatibility rejection, and concurrent publication conflict preservation in both local and Kubernetes compositions.
- (decision) Current brownfield grounding is adopted: CodeReconService maintains base/worktree indexes and exposes ensure, reindex, sync, merge_index, and drop_worktree semantics; CodeRecon tools are injected into sessions; runtime handoff builds resume/follow-up context from durable session summary/log artifacts and changed-file trail data; context.handoff is canonical; local session-handoff files remain in the Git common directory across worktrees and sessions.
- (decision) PostgreSQL and S3 remain absent from the baseline; AD-1 through AD-35 and CAP-1 through CAP-24 remain stable and are not renumbered or weakened.
- (decision) Reviewer fix for AD-36: repositoryIndexRequirement is a configured required|optional policy under the normal installation-to-job precedence and defaults to required; required waits with explicit IndexUnavailable/RebuildRequired state, optional records ProceedingWithoutIndex, so missing policy never silently degrades.
- (decision) Reviewer fix for AD-36: tenant-shareable means reuse only among attempts and overlays of the same immutable tenant identity; index bytes remain under tenant UID-derived storage and are never shared across tenants. Canonical identity includes generationKind base|overlay; overlayIdentity is forbidden for base and required for overlay, eliminating null/sentinel divergence.
- (decision) Reviewer fix for AD-36 lifecycle: lease acquisition and GarbageCollecting transition are competing resourceVersion-preconditioned writes; GarbageCollecting rejects new leases; deletion rechecks zero references/leases, drains in-flight queries, and for overlay generations waits for owning workspace deletion or retained-outcome detachment. merge_index atomically publishes successor routing and marks source Stale; drop_worktree release becomes collectible only after these gates.
- (decision) Reviewer fix for AD-37 selection: packageSequence is a per-lineage monotonic sequence allocated through canonical HistoryPort and preserved across backup/export/import; package ID is final tie-breaker, never Kubernetes timestamps/resourceVersions. Selection is CAS-bound to attempt UID and claim generation before credentials, and concurrent reuse requires explicit fanOut policy or a descendant package.
- (decision) Reviewer fix for AD-37 predecessor ordering: predecessor terminal handoff and repository-context publication, or a durable explicit PublicationFailed/no-context authorization, is recorded in the job projection before a successor claim may materialize context or receive credentials. Cleanup cannot revoke access or delete workspace before publication reaches that durable state.
- (decision) Reviewer fix for AD-37 repository context: canonical conflict detection auto-merges only changes to distinct canonical relative paths or distinct append-only record IDs; divergent bytes for the same path/record ID conflict. Sibling order is canonical by content hash then generation ID. An authorized operator or tenant_admin resolves conflict through a preconditioned RepositoryContextPort resolve operation that names all parent candidates and publishes a descendant; bounded pointer/Conflict status lives on CodePlaneRepositoryBinding.
- (decision) Reviewer fix for backup and import: backup quiesce fences every tenant-gateway mutation including worker-originated writes, carries a required absolute deadline whose expiry fails the epoch and releases scheduling, and validates snapshot-critical spec/projection/storage/handoff/context heads rather than unrelated status resourceVersion churn. Optional index imports revalidate destination schema, feature, model/tool and bytes before Ready.
- (decision) Reviewer fix for retention: holds are derived/recomputed from explicit attempt, follow-up job, import/export/backup and descendant references; deletion uses a preconditioned tombstone and immediately revalidates references and legal hold before physical deletion. Removal of a referencing object cannot race physical deletion.
- (decision) Current-fit review resolution: AD-36/AD-37 are target architecture delivered through AD-26, while current local CodeRecon path-keyed mutable ReviewKit and summary plus changed-files handoff are brownfield seeds that must migrate to the shared contracts before claiming conformance. FastAPI 0.136.3 is confirmed in uv.lock with supported range >=0.115,<1.
- (decision) Focused reviewer findings outside the requested index/handoff ownership change that would alter unrelated accepted AD-1 through AD-35 remain unchanged; existing AD-31/AD-34 already require history-ahead repair and CSI-fenced exclusive takeover.
- (event) Headless UPDATE: user approved a simplified Kubernetes v1 baseline. AD-1 through AD-37 IDs are preserved and amended in place; no AD-38 is added because every approved change fits an existing decision; the entire spine is re-distilled so no obsolete multi-tenant/gateway baseline claim survives.
- (decision) AD-4 amended: one single-replica CodePlane control-plane StatefulSet per installation/namespace runs FastAPI, orchestration/controllers, and local-style persistence services. The installation/namespace is one trusted tenant and the single trust/storage boundary. Baseline is non-HA and does not claim hostile same-installation multi-tenancy. The prior team-tenant-per-namespace multi-tenant model and per-tenant enrollment are removed from baseline. Local mode retains its single implicit tenant.
- (decision) AD-10/AD-11/AD-12/AD-33 amended authority rule: SQLite is canonical state/history/idempotency on a private RWO PVC mounted only by the control-plane. CRDs remain only for externally observable desired/current lifecycle and attempt ownership; CRDs are projections/desired inputs reconciled against SQLite, never a peer system of record.
- (decision) AD-10/AD-34 amended: one installation-scoped RWX data PVC with canonical layout /repos/<repo-id>/mirror.git, /repos/<repo-id>/workspaces/<attempt-id>, /repos/<repo-id>/indexes/<identity>/, /repos/<repo-id>/session-handoff/, /sessions/<job-id>/<package-sequence>.json, /artifacts/<job-id>/... . These exact paths are used unless a compelling documented equivalent replaces them.
- (decision) AD-8/AD-13/AD-24 amended: worker Pods and indexer Jobs mount only required subPaths in appropriate ro/rw modes. Each attempt has a private clone/worktree directory that only its worker mounts; the shared mirror may be mounted read-only to workers. Repository/ref publication and mirror mutation serialize via a Kubernetes Lease while retaining Git expected-head/CAS. Never depend on portable flock.
- (decision) AD-21/AD-34 amended CSI qualification: the operator-selected CSI must be qualified for RWX, POSIX atomic rename, durability after fsync, and the required subPath semantics.
- (decision) AD-10/AD-11/AD-31 amended writer ordering: artifacts and handoffs are ordinary RWX files referenced by SQLite. Writer ordering is temp file, fsync file, atomic rename, fsync parent directory as needed, then the DB reference transaction. Single-writer SQLite transactions retain canonical state/history/idempotency.
- (decision) AD-17/AD-31 amended backup/restore: backup pauses scheduling and all mutations, drains/parks operations, checkpoints SQLite, flushes both volumes, snapshots/copies the RWO and RWX volumes, and binds the independent captures in a checksummed manifest with explicitly no cross-volume atomicity. Restore restores both volumes, validates the manifest/hashes, invalidates active attempts/fences and clears Leases, reconciles CRD projections, and resumes only after validation.
- (decision) Removed from baseline everywhere (concise conceptual summary moved to a Deferred future HA/multi-tenant profile): tenant storage gateway and takeover/fencing; CodePlaneStorageShard; CodePlaneRepositoryIndex CRD; CodePlaneSessionHandoff CRD; dedicated tenant CodeRecon query service; immutable repository-context generation service; materialization/publish controllers; reference-counted leases; distributed cache GC; active/active API/controller baseline; multi-tenant UID-derived storage and cross-tenant guarantees within one installation; distributed projection/history machinery existing solely for multiple writers. Contradictory diagrams, tables, acceptance, packaging, prerequisites, migrations, capability mappings, assumptions, and export/backup text are removed.
- (decision) AD-36 simplified: repository-derived intelligence is filesystem cache directories on the RWX PVC with a small manifest containing repo, commit/tree, schema, config, tool identity, and checksum. One indexer Job holds a repository index Kubernetes Lease, builds in a temp dir, fsyncs, and atomic-renames to publish. Workers mount the published index read-only and query directly via injected CodeRecon tools; no index CRD or query service in baseline. Attempt/worktree deltas live under the attempt workspace or a private overlay. reindex/sync/merge/drop are locked filesystem ops. A GC controller scans under the index Lease based on active workspace/attempt references plus retention age, quarantining before delete. Index is optional/rebuildable and omitted from authoritative backup validity.
- (decision) AD-37 simplified: handoff state is immutable JSON/artifact files at the /sessions canonical layout; SQLite/history stores sequence, relative path, hash, lineage, compatibility, and selected attempt; no handoff CRD. The selected handoff is supplied as a read-only subPath or control-plane injection before execution; selection is CAS-bound in SQLite/history to attempt ID and fence. Repository protocol files live directly at /repos/<repo-id>/session-handoff/ and are mounted at the expected Git common-dir path. Append-only conventions are preserved; mutable singleton files use a short repository-context Kubernetes Lease plus temp/fsync/atomic rename. Conflicting candidates are preserved as conflict files and surfaced, with no generation CRD or service. Pod loss cannot lose committed files.
- (decision) AD-15 amended: the scale envelope is honest for a single control-plane replica (single-writer SQLite throughput, one indexer/attempt concurrency bounds, bounded CRD projection counts) rather than a multi-replica cluster claim.
- (decision) AD-17 amended: HA availability claims are removed. The single-replica control-plane baseline documents crash-and-restart recovery, RTO/RPO for Pod/node/logical failure while the CSI backend survives, and a brief planned-restart unavailability window; it does not claim 99.9 percent HA. Cluster/storage-backend disaster recovery remains a separately qualified operator profile. Local mode keeps crash-consistent restart.
- (decision) AD-18 amended: correlation no longer requires a tenant ID because the baseline has one installation tenant. Correlation uses installation ID plus job/session/event/request/execution IDs; audit remains immutable application data on an integrity chain.
- (decision) AD-33 amended closed CRD inventory: retain only externally observable lifecycle/attempt/ops resources actually justified. Keep CodePlaneJob, CodePlaneExecutionAttempt, CodePlaneApproval, CodePlaneRepositoryBinding, CodePlaneBackupEpoch, and CodePlaneImportSession (all projections/desired inputs reconciled against canonical SQLite). Remove CodePlaneTenantBinding (single trusted tenant = the installation namespace), CodePlanePolicyBinding (policy in SQLite/config), CodePlaneRepositoryIndex, CodePlaneSessionHandoff, CodePlaneStorageShard, CodePlaneRepositoryRefLock (repository/ref serialization is a Kubernetes Lease plus SQLite intent plus Git CAS), and CodePlaneOperation (idempotency ledger is single-writer SQLite). Ordinary file/index/handoff metadata is SQLite plus files, not CRDs.
- (decision) AD-34 amended (ID preserved) becomes the shared-volume durability boundary: the private RWO PVC holds canonical SQLite state/history/idempotency mounted only by the single control-plane replica; one installation-scoped RWX data PVC holds repos/workspaces/indexes/session-handoff/sessions/artifacts; worker Pods and indexer Jobs mount only required subPaths in ro/rw; single-writer SQLite plus temp/fsync/atomic-rename/parent-fsync file ordering is the durability contract; Kubernetes Lease plus Git CAS serialize mirror/ref publication. No tenant storage gateway, no shard takeover/fencing.
- (decision) AD-35 amended (ID preserved) becomes single-installation control authority/topology: one single-replica control-plane StatefulSet in the installation namespace owns FastAPI, orchestration, controllers, reconciliation, scheduling, and canonical persistence; human OIDC authorization stays application-level while Kubernetes RBAC governs the control-plane and worker service identities; the CRD OCI lifecycle stays explicit rather than delegated to ordinary Helm rollback. No installation bootstrap/tenant-enrollment controller and no active/active controller baseline.
- (decision) Security/trust posture: one installation namespace is one trusted tenant, so sibling jobs are not claimed as mutually hostile storage tenants. subPath mounting prevents casual sibling-workspace access, while the shared mirror and repository-context are intentional shared surfaces. Worker identity, service account, network policy, secret handling, and the callback fence are all preserved. Stronger tenant isolation, active/active control-plane, and per-tenant gateways are moved to a Deferred future HA/multi-tenant profile.
- (decision) Preserved invariants: local mode independence, supervised default, server_restart resume-in-place, TraceForge dotted event semantics, one Pod per attempt, absence of PostgreSQL and S3, absence of any external DB/object store, and the egress gateway, worker service-account, network, secret, and callback-fence protections all remain. AD-1 through AD-37 are not renumbered or weakened beyond the approved simplifications; CAP-1 through CAP-24 remain stable.
- (decision) AD-23/AD-26/AD-28 amended for consistency: export/import operates over CRD projections plus SQLite canonical history and RWX files (no gateway, no index/handoff CRDs); migration phases drop tenant-gateway/shard/index-service/publish-controller work and add the RWX-volume file layout, subPath mounts, Lease-based serialization, and SQLite-canonical adapters; acceptance criteria and the capability map are re-pointed to the surviving CRDs and single-replica topology.
- (event) Spine fully re-distilled to the single-tenant single-replica Kubernetes v1 baseline: AD-1 through AD-37 preserved and amended in place (no AD-38); closed CRD inventory reduced to CodePlaneJob, CodePlaneExecutionAttempt, CodePlaneApproval, CodePlaneRepositoryBinding, CodePlaneBackupEpoch, CodePlaneImportSession; SQLite made canonical over CRD projections; two-volume (private RWO + installation RWX) durability with subPath mounts and Lease/Git-CAS serialization; AD-36/AD-37 simplified to filesystem cache dirs and immutable /sessions handoff files with no index/handoff CRD or query/generation service; removed baseline machinery summarized under a Deferred future HA/multi-tenant profile. Deterministic lint (lint_spine.py) passes with zero findings. Re-distillation is ready for the independent reviewer gate; the spine is NOT yet finalized (status: draft).
- (decision) Reviewer-gate fix 1 (RWOP): the private SQLite state PVC uses ReadWriteOncePod everywhere (not ReadWriteOnce). RWOP is GA/stable across supported Kubernetes 1.34-1.36, so at most one Pod can mount it; AC-3 proves a second Pod cannot concurrently mount the RWOP private state PVC.
- (decision) Reviewer-gate fix 2 (authority/B-1): CodePlane CRD spec and status are projection-only externally observable lifecycle views written from committed SQLite and are never read as behavioral input. No kubectl/GitOps/direct CRD mutation is accepted as product intent; external spec/status writes are reverted to the SQLite projection or rejected on the next reconcile. All commands enter the authenticated API and commit to SQLite first; the reconciler derives desired state exclusively from SQLite. Field-ownership text, CRD table, dual-mode table, and diagrams are aligned to projection-only.
- (decision) Reviewer-gate fix 3 (restore/B-2): add a durable monotonic installation incarnation (projection epoch) persisted in SQLite, rotated on every restore. Attempt fences, callback credentials, SSE cursors, and CRD projections all bind the incarnation. Restore authoritatively deletes/recreates or overwrites surviving CodePlane CRs under a codeplane-restore field manager after preserving old metadata as provenance; the AD-31 projection-ahead fail-closed guard is suspended only inside the restore barrier and re-arms at the new epoch. etcd CR state is excluded from the two-volume backup set and always rebuilt from SQLite; pre-restore workers and cursors can never validate.
- (decision) Reviewer-gate fix 4 (fence/B-3): define exactly one worker wire value attemptFence = (installation incarnation, monotonic claim generation, attempt UID). Every callback and every file/publication commit compares message.attemptFence to the current SQLite value transactionally in the same transaction before any append. Advancing the fence means minting a new claim generation (which produces a new attempt); no in-place re-fence. The ambiguous callbackEpoch / attempt-callback-fence terms are removed.
- (decision) Reviewer-gate fix 5 (idempotency digest/N-1): the canonical request digest is a versioned canonical encoding binding operation kind, canonical payload, actor/service identity, installation incarnation, target IDs, expected versions/fence, and accepted policy generation. Replays under a different actor, policy generation, or incarnation fail the digest rather than returning the stored result.
- (decision) Reviewer-gate fix 6 (mirror safety/B1): only the single RWOP-bound control-plane repository-acquisition adapter may mutate the shared bare mirror. The repository-mutation Lease is coordination/liveness, not fencing. Mirror mutation uses Git lock files plus ref expected-head CAS, verifies with git fsck before publication, never prunes/repacks while active workspace references exist, and on interruption quarantines and rebuilds from the remote or last verified mirror before reuse. Indexers and workers mount the mirror read-only and never mutate it. One canonical mirror path is kept.
- (decision) Reviewer-gate fix 7 (backup barrier/B3): the backup barrier is enforced in canonical SQLite, not by cross-Pod clocks. The controller rejects new scheduling/publication, drains/parks active operations, revokes publish credentials, stops/deletes indexer Jobs, waits for workers to acknowledge pause/terminate, and verifies via the Kubernetes API that no non-control Pod retains an RW mount to authoritative subPaths before capture. Deadline expiry aborts capture and resumes safely; it never snapshots after timeout. Workspace writes are excluded only if explicitly non-authoritative. Then checkpoint/flush and capture; preserve no cross-volume atomicity.
- (decision) Reviewer-gate fix 8 (orphan reclamation/N1,N2): a startup/retention scan quarantines stale temp and unreferenced /artifacts and /sessions files that no SQLite reference names, after an age threshold, rechecks under the appropriate Lease, then deletes. Multi-part handoff packages commit all referenced blobs durably before the manifest/package JSON (manifest-last) or the package is a single self-contained JSON file. The attempt workspace path is keyed by the immutable attempt UID and never reused; durable SQLite active/retained references govern GC with quarantine and recheck before delete.
- (decision) Reviewer-gate fix 9 (storage qualification): CSI exposes no POSIX/durability capability flag, so qualification is an executable post-install conformance probe run across nodes and Pods that proves RWX read-after-fsync visibility, temp-file plus atomic-rename visibility, crash/remount durability, directory-fsync behavior, and the required kubelet subPath mount/reconnect behavior. Installation fails if the probe or documented backend guarantees cannot establish them. subPath is kubelet/platform mount behavior, not a CSI capability. Async/tiered or object-backed filesystems that acknowledge writes before durable persistence are disallowed. Stale mounts are monitored.
- (decision) Reviewer-gate fix 10 (security honesty): AC-4 softened to match AD-30. Declared subPath mounts prevent accidental/casual sibling access, not malicious same-installation storage isolation; intentional shared surfaces (mirror, repository-context) are documented, not falsely isolated. Baseline workers set automountServiceAccountToken false and get no Kubernetes API access; any adapter needing API access is a separately reviewed profile with per-attempt-scoped RBAC, not baseline. Residual risk is named. No overclaim of symlink/TOCTOU resistance: the worker cannot control the volume root/subPath targets, but stronger hostile-job isolation is Deferred.
- (decision) Reviewer-gate fix 11 (rubric): add AD-3 to AC-4 and AD-26 to AC-13 governing decisions so every AD governs at least one acceptance criterion; remove the 'operation' CRs phrase from AD-15 GC wording because the operation ledger is single-writer SQLite (only attempt/approval terminal CRs archive-then-GC). Capability-map reverse references reconciled where bindings changed.
- (decision) Reviewer-gate fix 12 (adjacent consistency): propagate RWOP naming, projection-only CRD authority, the single attemptFence value, and installation-incarnation binding through the sequence/topology/security diagrams, the CRD ownership contract, the durable-data and dual-mode tables, the consistency conventions, and the backup/restore steps and ACs so no adjacent contradiction survives fixes 1-11. Current-fit facts preserved: Kubernetes 1.34-1.36, Helm chart API v2 / CLI 3.21.x and 4.2.x, FastAPI 0.136.3, VolumeSnapshot v1, Lease v1; CSI POSIX/durability remains empirically qualified, not a capability flag.
- (event) Final shared-volume reviewer gate PASS: all Blocking/High findings in review-data-integrity-shared-volume-final.md, review-incompatibility-authority-shared-volume-final.md, and review-rubric-shared-volume-final.md are closed; deterministic lint reports zero findings.
- (event) spine finalized
- (decision) Final gate fix: before backup capture, terminate workers or otherwise remove their mounts and verify through the Kubernetes API that no non-control Pod retains any read-write mount to the installation RWX PVC; checking only authoritative subPaths is insufficient, and timeout always aborts capture.
- (event) Final shared-volume reviewer gate PASS: all Blocking/High findings are closed after strengthening backup quiescence to require absence of every non-control RWX read-write mount; deterministic lint reports zero findings.
- (event) spine finalized
- (decision) User correction: CodeRecon indexes and session handoffs are representative shared-file cases, not platform primitives. The spine must define one generic installation-RWX shared-file lifecycle and must not elevate either feature into bespoke infrastructure terminology.
- (decision) AD-10, AD-16, AD-23, AD-26, AD-28, AD-30, AD-31, AD-33, and AD-34 are amended around one generic SharedFileStoragePort only where an application port is needed. SQLite owns metadata, references, policy, retention state, hashes, and provenance; the installation RWX volume owns bytes. RepositoryIndexStoragePort and feature-specific handoff/context storage infrastructure are removed as mandatory architecture primitives.
- (decision) Every shared file set is classified by scope installation|repository|job|session|attempt; lifecycle authoritative-shared-data|durable-artifact|derived-cache|disposable-workspace; designated writer and readers; immutable|append-only|mutable-singleton behavior; content hash/reference requirements; atomic publication; retention/cleanup; backup-included versus rebuild/excluded behavior; and allowed Pod subPath/access mode.
- (decision) Canonical RWX layout is /repos/<repo-id>/mirror.git, /repos/<repo-id>/workspaces/<attempt-uid>/, /repos/<repo-id>/shared/<artifact-type-or-relative-path>/, /jobs/<job-id>/artifacts/<artifact-id-or-name>, /jobs/<job-id>/sessions/<session-id-or-sequence>/..., and /cache/<scope>/<identity>/... . There are no top-level /sessions or /indexes namespaces.
- (decision) AD-36 is generalized in place to derived shared data and caches. Derived datasets are identity-manifested, checksum-verified, atomically published filesystem files/directories on RWX, rebuildable and non-authoritative, read-only to consumers, and garbage-collected with Lease-serialized quarantine and reference recheck. CodeRecon indexes, build outputs, analysis caches, generated indexes, and future derived datasets are examples under one contract.
- (decision) AD-37 is generalized in place to durable shared artifacts and repository-scoped shared files. Durable artifacts are immutable or append-only with SQLite path/hash/provenance references and type-specific compatibility only when needed. Mutable repository-scoped singleton files require designated-writer ownership, a short Kubernetes Lease for cross-Pod publication, temp/fsync/atomic rename, and conflict preservation rather than last-writer-wins. Session summaries, handoff packages, plans, logs, checkpoints, test results, exports, generated reports, and repository session-handoff records are examples. Handoff selection/fencing is only an application rule when a successor consumes that artifact.
- (decision) Backup/export include authoritative shared data and durable artifacts; derived caches are rebuildable and optional; disposable workspaces are excluded unless explicitly retained and reclassified as durable artifacts. Restore rebuilds SQLite references and validates included bytes; missing derived caches trigger rebuild, not degraded authority.
- (decision) Deferred HA/multi-tenant work may later choose distributed shared-storage metadata or services generically. Dedicated CodeRecon, index, handoff, or repository-context CRDs/services are not inevitable future architecture. The simplified baseline remains unchanged: one trusted tenant namespace, one non-HA control-plane StatefulSet, canonical SQLite on private RWOP, one installation RWX PVC, projection-only CRDs, one Pod per attempt, one attemptFence, no worker API token, control-plane mirror mutation, full non-control RW-mount backup barrier, no PostgreSQL/S3/external state service, autonomous local mode, supervised default, server_restart, and TraceForge dotted events.
- (decision) Final shared-artifact review fix: authoritative shared data now has scope-appropriate canonical shared namespaces and immutable-generation plus SQLite pointer-CAS publication; structured mutable stores may use a complete domain-native atomic protocol, with the AD-8 Git mirror as the sole baseline specialization.
- (decision) Final shared-artifact review fix: mutable singleton publication never replaces bytes behind a committed reference; new immutable generations are fsynced and verified before SQLite CAS, conflict candidates are preserved, and old generations GC only after reference/hold/mount rechecks.
- (decision) Final shared-artifact review fix: the shared Git mirror is explicitly repository-scoped authoritative shared data for acquired installation state, uses control-plane-only Git-native lock/object-fsync/ref-CAS/fsck/quarantine semantics, is backup-included, and retains remote authority for published refs.
- (decision) Final shared-artifact review fix: vocabulary is uniform: SQLite is metadata/reference/publication-state authority, RWX is byte custody/durability substrate, SharedFileStoragePort is the operation boundary, and application ports own domain policy.
- (decision) Final shared-artifact review fix: derived-cache GC CAS-transitions ready to deleting before quarantine, rejects new references/mounts, checks zero references/holds/mounts under the Lease, persists phases, and restores or deletes without identity reuse races.
- (decision) Final shared-artifact review fix: RWX layout migration uses a resumable SQLite ledger, copy/fsync/hash verification, pointer CAS, N/N-1 compatibility window, restart adoption, rollback gates, delayed quarantine/GC, and backup exclusion while a pointer switch is in flight.
- (event) Final shared-artifacts reviewer PASS: no Blocking or High findings remain; abstraction quality, feature-specific leakage, data ownership, and lifecycle compatibility gate passed. Deterministic lint reports zero findings.
- (event) spine finalized
